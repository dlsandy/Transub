# Changelog

## 3.0.2

### BUG修复

- 修复字幕编辑器最小化（或关闭）后，主窗口内容变空白的问题
- 修复字幕编辑器无法打开 ASS / SSA 字幕文件的问题（支持导入编辑并保留样式头）
- 修复智能翻译过程中引擎超时重试撞上「adapter busy / HTTP 429」导致整单失败的问题（适配器排队 + 引擎忙等待重试；智能翻译 HTTP 超时延长至 30 分钟）
- 修复任务失败 / 取消 / 异常中断后 `llama-server` 与引擎进程常驻后台的问题（批次结束回收本地 LLM；失败或中断时同时停止引擎；关闭窗口时强制回收）
- 修复译中把完整对白软省略成「嗯， / 啊 / 请」等语气词后仍视为有效译文的问题（欠译判定；推理翻译对可疑/空白条逐条补译；智能翻译经适配器清洗）
- 修复合并双语 ASS 时译文/原文上下行顺序与设置不一致的问题

### 新功能

- **llama-server 可选 CUDA 后端：** 设置 → 运行环境可在 Vulkan与 CUDA 12（NVIDIA）（默认）之间切换；
- **应用内更新：** 同时检查 GitHub / Codeberg；下载 zip 前测速选较快线路；主程序退出后显示独立安装进度窗口

### 优化

- 扩充日语 ASR 域纠错词表；
- 加强译后清洗（叠名 / 敬语误译、提示词泄漏、跨条 stuck 译文、软省略欠译等）；人名术语 harvest 更保守
- WhisperSeg 等按本机 CUDA 12 / 13 自动匹配 onnxruntime-gpu，降低灵敏检出掉到 CPU 的概率
- 模型 / 运行时 / 应用更新下载支持多镜像测速
- **感知交互：** 任务列表感知徽章可点击切换采纳 / 不采纳；「重感知」改称「深度感知」

### 关于Pro版本

Pro版本主要针对大模型和智能化做了提升，但对用户的硬件配置也有更高的要求。

免费版本已满足大部分的用户，翻译质量和语音识别、断句以及字幕编辑器的功能均已经达到非常精准的程度。

非硬性需求者，**请谨慎考虑开通Pro功能（硬件不达标的用户就算开了也会跑不起来）**，Pro功能开通后不支持退款。



### 其它

- **推荐安装**：下载 `Transub-3.0.2-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-3.0.2.exe`
- **升级**：建议使用软件内检查更新完成升级（无需重复下载运行环境和模型）

## 3.0.1

### BUG修复

- 修复智能感知错误识匹配日语影片翻译模型的bug
- 执行新任务时会自动清空任务列表非此批任务的bug

### 新功能

- **快捷一键修复：** 生成任务执行完成后，可点击一键修复QC按钮将列表内存在QC问题的字幕一键修复，或通过列表内单个字幕后面的扳手图标一键修复QC
- 增加了自动升级检测功能（入口在设置 > 更多 > **自动检查更新**）
- **本地大模型手动放置校验：** 校验 GGUF 文件头与体积，识别下错/下残文件；目录内发现近似文件时提示正确文件名；设置页对异常文件显示「文件异常」
- **单文件 GGUF 手动下载：** 大模型目录与处理模型卡片增加「手动下载」；说明文件名与应放置目录（`advanced-llm/models`），支持打开下载链接 / 打开目录 / 放入后检测
- **智能感知加强：** 扩大文件名识别范围（动画/综艺等）；快速感知结果为「未识别」时自动加深一次（短窗语种 + 声学），并据声学/片长证据提升类型匹配
- **番号识别：** 独立 `CODE-123` 文件名（如 `MNGS-057`）即使厂牌未入库也按日语 AV 即感；短窗语种误检英语 / 软声声学不再把它抬成「对白」

### 其它

- **升级**：建议使用软件内检查更新功能完成升级（无需重复下载运行环境和模型）
- **推荐安装**：下载 `Transub-3.0.1-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-3.0.1.exe`

**注意：如需继续使用TransWithAI**外部引擎，请下载2.0.0版本（3.0版本不再支持该引擎）

## 3.0.0

**（此版本增加和修改的功能和项目非常多，难免存在BUG，欢迎发帖告知，我会在第一时间进行修复优化）**

重大升级版本：默认处理引擎改为内置 **Transub Engine，新引擎极大的提升了转录和翻译质量并支持大模型接入。 全新的智能感知功能，自动识别拖入的音视频并自动匹配内置感知参数，使用起来更简单，效果更好。**全面优化和增强Transub Editor字幕编辑器功能，提供了诸多全新的功能，并大幅减轻长片编辑卡顿。且带来了自定义布局功能，适应各种不同喜好的字幕编辑布局习惯。

**首次使用建议使用向导完成系统检查和设置。**

产品对比表见 [README.md](README.md#compare-2-vs-3)。边界说明：[docs/engine-boundary.md](docs/engine-boundary.md)、[docs/advanced-boundary.md](docs/advanced-boundary.md)。

### 相对 2.0：功能速览


| 主题    | 2.0            | 3.0                                                      |
| ----- | -------------- | -------------------------------------------------------- |
| 默认引擎  | 外装 TransWithAI | **内置 Transub Engine**                                    |
| 素材配参  | 开始前可选匹配        | **按项自动感知**（类型即感 / 语种 / 声学 / 纠错记忆）                        |
| 免费译中  | 依赖已有路径，以日译中为主  | **机器翻译 + 推理翻译（支持多语言）**；忠实语气                              |
| 智能翻译  | —              | **Pro 专属**（影片简要理解 → 分块译 → 保持一致性）                         |
| Pro   | —              | 语境/影片重构、影视音频增强、语义审阅、BYOK/大模型、ASS 说话人样式；**大版本内买断**（爱发电领取） |
| 编辑器   | 双语 / 预设 / 工作流  | + 工作区模式、书签/A-B、说话人与审校、原文缓存、导出清单、规则审阅等等数十项加强              |
| 下载与更新 | 分散             | **统一下载中心**；zip/NSIS **应用内更新**（自动保留已下载的 支持库、模型、推理文件）      |


### 相对 2.0：性能与体验


| 维度      | 3.0 改进                                   |
| ------- | ---------------------------------------- |
| 拖入感知    | 文件名快感优先；快速匹配，深入探测按需「重感」                  |
| 长片列表    | ≥180 条字幕局部加载；降低性能占用，减少卡顿                 |
| QC / 查找 | 长片改走 **Web Worker**                      |
| 撤销      | **Undo 补丁**相对基线，降低内存占用                   |
| 显存      | ASR→MT 前释放；本地 LLM **空闲约 5 分钟停服**，避免重构后卡顿 |
| 生成器界面   | 4K **界面缩放**；任务资源占用条（CPU/内存/GPU）          |
| 安装设置    | 向导式安装，减少复杂的参数设置                          |
| 启动      | 可设置默认启动项为编辑器或生成器                         |


### 新功能

#### 引擎与生成

- **内置 Transub Engine 引擎**
- **按项自动感知**：拖入后分析类型（情景剧 / 影视 / 访谈）、语种、ASR·MT、VAD/增强；有结果默认采纳；缺项可引导安装
- **类型即感 / 深入重感**：拖入只做文件名快感；匹配度强立即采纳；「重感知」才跑语种 / 声学 / 纠错记忆（设置中可开启默认重感知）
- **短窗语种探测**（引擎 `detect-language`）+ **声学轻探测** + **感知纠错记忆**（同目录偏好）
- **翻译三选一**：引擎 Opus（多语→中）/ **推理翻译（Sakura 等，免费）** / **智能翻译（Pro）**
- **智能翻译（Pro）**：影片简要 → 分块译 → 一致性；经引擎 `mtBackend=external`；可与 LLM 推理分用途选模型
- **影视音频增强（Pro）**：Demucs 人声分离 + 电影级 VAD；免费侧保留更激进切分 / 轻度降噪
- **灵敏检出**（WhisperSeg）、幻觉静音阈值等
- **译后清洗**：叠名幻觉、句末臆造人名、术语统一、提示词回声等（引擎 + 桌面双层）
- 字幕默认同名写出（不再强制 `.zh.srt`）；引擎支持 ASS / 合并双语 `*.dual.ass`
- **统一下载中心**（模型 / GPU / Demucs / GGUF / 应用更新）、**设置向导**（独立窗口）、任务资源占用条、界面缩放（4K）
- **zip / NSIS 应用内更新**：保留 `transub-engine/models`、`advanced-llm` 与已装 GPU / Demucs 等 site-packages

#### 字幕编辑器

- 工作区：**精修 / 时间轴 / 双语 / AI / Pro**（`Alt+1…5`）；模式工具条直接露出
- **书签 / A-B 循环**、说话人与审校状态（未看/已改/已通过）、建议说话人
- **原文缓存**：挂副轨、Diff、选区/全文恢复误删；重构/翻译可注入 source
- **导出前检查清单**、双语规则审阅、**双语语义审阅（Pro）**
- 列表窗口化（≥180）、Undo 补丁、QC/查找 Worker、时间轴磁吸、真 ASR 置信度写入 `.transub.json`
- **语境重构 / 影片理解重构（Pro）**：Film Brief 预览、对照确认、失败块重试、书签段工作流
- ASS 说话人样式导出（Pro）；情境条快捷 Pro 动作

#### Pro 与许可

- **大版本内买断**（设备绑定；覆盖本主版本如 3.x）；[爱发电](https://afdian.com/a/transub) 订单号「领取并激活」或粘贴 `TSUB1.…` 密钥
- 大模型：BYOK 或软件内 GGUF + llama-server（Vulkan）
- 许可说明见 `LICENSE` / `LICENSE-PRO` / `NOTICE`

#### 特别感谢

网友huihuil提供大量宝贵的意见和大量测试，使得软件功能更完善和更少的bug。再次表示感谢！

### 其它

- **推荐安装**：下载 `Transub-3.0.0-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-3.0.0.exe`

## 2.0.0

### 重大更新

- 软件架构进行了大量的优化，使性能更进一步提升。
- 重构后的界面，将常用操作集成在主窗口内，使操作更便捷。
- 增加大量实用新功能（双语字幕、字幕预设、工作流等）。

### 新功能

- **双语任务**：单次任务即可输出原文和译文，可选合并输出双语字幕；（双语功能需分别下载和设置转写/翻译模型）
- **导出繁体中文字幕**：主界面设置目标为繁体中文时，可生成繁体中文字幕
- **独立的字幕编辑器启动项**：可通过Transub Editor图标快速启动字幕编辑器，快速查看/编辑历史字幕文件
- **字幕预设**：编辑器支持按时段批量插入片名/演员/字幕作者等预设文案
- **工作流**： 创建你自己的工作流，将重复流程用工作流快速批处理。（已内置两套常用工作流）
- **双语字幕编辑**： 支持双语字幕编辑，可设置原文和译文的上下位置，支持双语字幕合并。
- **任务历史：** 支持历史任务的查看、编辑和清除

### 性能 / 启动体验

- 优化了软件冷启动速度
- 优化了任务文案，更好的理解任务状态
- 优化了字幕编辑器波段操作时的卡顿现象
- 重构了主界面UI，各项操作、信息更一目了然。
- 重构了设置界面，分为标准和专家两种设置模式。新手不必再为看到一堆复杂的设置项头痛了。
- 配置预设，可将各项设置项进行存储，便于各种不同情况下快速切换配置项。

### 其它

- **推荐安装**：下载 `Transub-2.0.0-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-2.0.0.exe`

## 1.4.4

### 修复

- **Smart App Control 拦截**：关闭 electron-builder 向 exe 写入 asar integrity（该改写会让未签名的 `Transub.exe` 在 Windows 11「智能应用控制」强制模式下被拦截）；仍保持未签名，图标/版本信息继续由 after-pack 写入

### 其它

- **推荐安装**：下载 `Transub-1.4.4-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.4.exe`

## 1.4.3

### 修复

- **打包缺词典**：`subtitle-chinese-core` 依赖的 `subtitle-chinese-dict` 未打入 `app.asar`，简繁转换仍会失败；打包清单改为纳入全部 `*-core.js` 与该词典

### 其它

- **推荐安装**：下载 `Transub-1.4.3-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.3.exe`

## 1.4.2

### 修复

- **打包缺模块**：正式包 `app.asar` 未打入 `subtitle-chinese-core`，批量完成后做简繁转换时报 `Cannot find module '../src/js/subtitle-chinese-core'`

### 其它

- **推荐安装**：下载 `Transub-1.4.2-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.2.exe`

## 1.4.1

### 新功能

- **压缩叠词**：将连续重复 ≥ 3 次的短语/单字压成「词…词！」（如「好的」×5 →「好的…好的！」），降低 CPS；编辑器工具菜单、当前条目工具栏、QC 一键修复均可用

### 设置

- **完成后压缩叠词**：设置 → 编辑 → 批量完成后；默认开启，写回字幕
- **完成后自动按 CPS 拆句**：默认改为开启（此前默认关闭）

### 字幕编辑器

- **列表全文显示**：字幕列表文本不再两行截断，长句可完整换行查看
- **QC 叠词检测**：质量检测可标出叠词嫌疑，一键修复可选压缩叠词

### 其它

- **推荐安装**：下载 `Transub-1.4.1-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.1.exe`

## 1.4.0

### 题外话

- **更新频率**：自项目开始至今几乎保持着每天更新的节奏，但目前各项功能都已较为完善且稳定。**后续会大幅度的降低更新频率**。
- **新功能**：目前各项功能已经非常强大，尤其是字幕编辑器的功能已经强于大部分同类字幕编辑器。目前软件所呈现出来的功能已经是我所能想到的几乎所有想法了。**所以对于新功能，更多的是需要依靠网友们的想法和建议了。**
- **MAC版本：**暂无MAC版本的开发计划（有开发MAC版本的开发者可借助项目开源代码只需微做适配即可制作出MAC版本）

### 新功能

- **波形编辑器：支持放大缩小波形尺寸，通过拖动、拉拽等方式快速调整字幕时长**
- 全局术语表：在翻译模式下，自动应用全局术语表（在设置内开启，默认关闭）
- 抑幻听：通过设置，可极大程度减少幻听、杂音、错误转录BGM等情况
- 自动拆句：对于单条过长的字幕按语句自动拆分（需在设置中 开启 **自动按CPS拆句**）
- 生成繁体字幕：现在可以直接生成繁体中文字幕了（在设置的输出项内的中文字幕类型，选择 繁体中文）

### 主窗口

- **独立设置窗口**：设置改为独立 BrowserWindow；从字幕编辑器打开设置时不再顺带唤起主任务窗口

### 字幕编辑器

- **波形默认开启**：打开编辑器时默认显示波形时间轴（仍可关闭；本地已保存的开关偏好优先）
- **时间轴/波形右键菜单**：在时间轴字幕块与波形对应区间上右键，可打开与字幕列表相同的上下文菜单（分割、对齐、时长调节等）
- **删除杂音**：新增「幻觉短句」规则（完毕 / ○○○ / 过短占位等）

### 设置

- **设置分区重排**：8 个页签收为 6 个——**常规**（预设/任务/设备/托盘）、**环境**（TransWithAI + FFmpeg）、**输出**、**转录**、**编辑**（编辑器 + 批量完成后）、**更多**；各区分组与说明文案更清晰，打开设置默认进入「常规」
- **中文字幕类型**：输出页可选简体中文 / 繁体中文（默认简体）；仅「翻译」任务生效，完成后用 OpenCC 做简繁转换（不再写入 initial_prompt，避免「简体中文」等提示被复述进字幕）；独立设置窗口保存后会同步到主窗口，后处理以磁盘配置为准，避免仍按旧的「简体」转换
- **抑幻听参数**：转录页可调无语音/对数概率/压缩比阈值，以及可选的幻听静音跳过；写入 TransWithAI `generation_config`，便于 BGM/静音较多时减少胡编字幕
- **内置预设「翻译 · 抑幻听」**：日语锁定 + 更严的无语音/压缩比阈值 + 较短合并上限（8s）与智能切分 20s，适合 BGM 或静音较多的内容
- **完成后自动按 CPS 拆句 / 清理杂音幻觉**：设置 → 编辑 → 批量完成后；可选写回字幕，缓解单条过长与「完毕」类短句
- **翻译句读后空格**：翻译任务完成后，在 CPS 拆句之前自动在中文 `。？！` 后补空格（已有空白/行末不重复），便于拆句识别句界
- **术语表写入提示**：默认将全局术语写入 `initial_prompt` / `hotwords`（generation_config）
- **参数试跑对比**：设置 → 更多；对勾选视频片头用两套预设各跑一遍并对比 QC

### 平台与分发

- **发行包调整**：处于 portable 版本的运行体验较差，故后续版本仅提供 `Transub-*-win.zip` 与 `Transub-Setup-*.exe`，不再提供 portable 便携版

### 其它

- **推荐安装**：下载 `Transub-1.4.0-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.0.exe`

## 1.3.2

### 平台与分发

- **打包资源封入 asar**：HTML / JS / CSS 不再解压到 exe 旁明文目录，改为打入 `app.asar`，降低被随意篡改的风险（`_internal` 等原生依赖仍保持旁路）
- **默认分发改为 zip + 便携版**：未签名时 NSIS `Setup.exe` 易被 SmartScreen / 杀软拦截；Release 主推 `Transub-*-win.zip`（解压即用）与 `Transub-*-portable.exe`。NSIS 安装包仅作可选本地构建（`npm run build:setup`），不再作为默认发布产物

### 主窗口

- **记住窗口尺寸**：关闭后再次打开恢复上次宽高、位置与最大化状态
- **新版本检查**：更加方便的检查软件新版本，快速用上新功能和bug修复

### 设置

- **默认参数调整**：与常用配置对齐——字幕格式默认仅 SRT；合并间隔 500ms / 最大时长 10s；默认不生成 .bak；托盘进度默认关；完成后 QC 检测默认开
- **系统托盘通知开关**：运行页可开关桌面通知（任务完成 / 最小化提示），默认关闭
- **检查新版本入口**：主窗口设置按钮旁增加图标，可直接检查更新

### 字幕编辑器

- **时间轴/波形可缩放平移**：默认 5× 显示；Ctrl+滚轮或按钮放大缩小，滚轮/滑条左右滚动；倍率写入本地偏好，下次自动恢复
- **新增时间轴/波形区字幕拖动调整时长**： 调整字幕时长更方便了
- **波形加载提示**：开启波形时显示进度遮罩与状态栏提示，避免长视频抽峰时界面无反馈
- **.bak 备份位置可配置**：设置 → 字幕编辑器；可选不生成（默认）、与字幕同目录、或软件数据目录下的 `backup` 文件夹
- **静音分割更精准**：粗定位 + `atrim` 精确取窗（避免关键帧偏移）；单声道高通预处理；切分点按「靠近文本断点 + 静音时长」评分；相邻条目按静音边缘分开
- **静音分割对齐断句词**：理想切点合并空格/换行、断句词与标点；连续无空格文本若匹配断句词也可静音分割

### 修复

- **确认框屏闪**：关闭确认/取消后不再 `blur` 再抢焦，避免短暂露出下层主程序窗口
- **任务栏图标**：开发模式 (`npm start`) 会把 Transub 图标写入 `electron.exe`，避免重装 Electron 后又变回默认图标；从托盘恢复时也会重新应用窗口图标；最大化改在 show 之后，避免任务栏显示宿主 exe 图标

### 其它

- **推荐安装**：下载 `Transub-1.3.2-win.zip` 解压后运行 `Transub.exe`，或使用便携版 `Transub-1.3.2-portable.exe`（均未签名，一般不会像 NSIS Setup 那样被系统拦截）

## 1.3.1

### 主窗口

- **托盘进度与 ETA**：任务进行中更新托盘提示；进度条文案显示已用时间与预计剩余（基于片长与历史吞吐）
- **托盘相关设置**：运行页可开关「任务时更新托盘进度」「开始时最小化到托盘」
- **完成后 QC 检测**（默认关）：设置 → 字幕编辑器；任务结束后仅扫描并标出问题数，不自动修复、不自动打开编辑器
- **启动加速**：窗口 `ready-to-show`、桥接按需加载；FFmpeg / TransWithAI / GPU 检测改为后台快速检查
- **任务队列**：并行探测视频；列表增量刷新；排队完成后确认是否继续
- **设置分区**：原「高级」改为「FFmpeg」；新增「更多」（导入/导出配置、检查更新、任务历史、访问官网）
- **检查更新**：安装包将主进程共用的 `src/js/*-core` 打入 asar，修复「检查软件更新」因缺少 `subtitle-glossary-core` 报错；下载进度条显示
- **取消任务**：Windows 下 `taskkill /T` 结束 infer 进程树
- **任务完成后**：可打开输出目录、播放提示音；GPU 建议可一键应用设备

### 字幕编辑器

- **多选批量**：Ctrl/Shift 点击多选；支持批量删除、合并相邻、简繁/偏移作用于选中；`Ctrl+A` 全选可见行
- **长任务可取消**：静音批处理 / 贴边 / 重转写进度条增加取消；Esc 可中止；FFmpeg 进程可终止
- **可选波形时间轴**：工具栏开关（默认关）；开启后从关联视频抽峰绘制在时间轴下方
- **自动备份与草稿恢复**：保存时写 `.bak`；脏状态定时草稿；重新打开时若草稿更新可选择恢复
- **项目术语表**：字幕旁 `{stem}.glossary.json` sidecar；编辑器术语表弹窗可切换全局/项目，扫描与统一使用合并后的有效术语表
- **简繁短语转换**：OpenCC 短语优先 + 字符映射；可选保护术语表词条（默认开启）
- **批量简繁转换**：工具菜单 → 文本 →「简繁转换」，支持简→繁 / 繁→简，可作用于全部或当前选中条目（可撤销）
- **Ctrl+↑/↓**：文字编辑框内切换上一条 / 下一条字幕
- **快捷键**：工具栏独立按钮（设置后方）

### 修复

- **设置不持久**：打包后用户设置改为写入稳定目录（NSIS → Electron `userData`；便携版 → `PORTABLE_EXECUTABLE_DIR`），不再写到每次解压的临时 exe 旁；并自动迁移旧位置的 `transub-settings.json`（感谢QQ804218提供的bug信息）
- **FFmpeg 目录“消失”**：自定义路径若落在软件安装/`_internal` 内，更新或重装会覆盖该树；现会提示改用安装目录外的路径，且检测保存失败时不再静默忽略
- **查找替换**：全部替换成功后显示「已成功替换 N 处」，不再误报「未找到匹配项」
- **确认框失焦**：确认/取消后恢复编辑器输入焦点（避免需切窗才能继续编辑）
- **任务栏图标**：开发与打包路径正确使用 Transub `app.ico`
- **日志滚动**：转录过程中，日志自动滚动至最新条目（感谢QQ804218的建议）

### 平台与分发

- **GitHub 应用更新**：设置页「检查更新」对接 GitHub Releases
  - NSIS 安装版：若 Release 含 `latest.yml`，可应用内下载并重启安装（electron-updater）
  - 便携版 / 开发模式 / 无 `latest.yml`：通过 GitHub API 比对版本并打开下载页

## 1.3.0

- 字幕编辑器：QC、通顺度、术语表、时间轴、静音/智能分割、局部重转写等
- 内置 FFmpeg；Windows 一键打包脚本

## 1.2.0

- 多种分割模式；共享分割核心；静音检测

## 1.0.x

- 初始基于 TransWithAI 的批量字幕生成与基础编辑

