# Changelog

## 3.1.3

### 新功能

- **扫描目录：**可在主窗口 自动添加 菜单项和按钮一键扫描缺字幕的音视频文件并添加到任务列表。（入口： 设置 > 扫描目录）
- **复用本机 CUDA 运行库：**安装 / 更新 CUDA 版 llama-server 时，白名单探测 NVIDIA Toolkit、`CUDA_PATH`、引擎内 pip `nvidia-*` 等处的 cudart/cublas；校验齐全后复制到本机运行时目录并跳过 cudart 下载（失败则回落官方 zip）。设置 → 运行环境提供「扫描本机 CUDA」手动探测，并可一键复用到运行时目录。**引擎 GPU 支持**同样会复用本机 Toolkit（跳过 nvidia-* 大包）；「扫描复用本机 CUDA」可复制进引擎 site-packages。

### 优化

- **Transub Engine：** 支持流式输出字幕，用于适配即将发布的Transub Player实时字幕播放器。

## 3.1.2

### BUG修复

- **任务中打开「设置 → 模型」会中断任务：** 刷新模型列表时若引擎健康检查短暂超时，会误杀正在跑的引擎进程；现任务进行中不再重启/结束引擎，并暂缓刷新列表。
- **批次后低置信二意见导致引擎崩溃（全版本）：** 长片 `anime-whisper` 后若立刻用 `kotoba` 等易崩 CT2 做局部重转，可能触发 `0xC0000005` 且批次中无法重启引擎。现二意见优先已安装的更稳模型（如 SenseVoice / Qwen），并把 anime/kotoba 放到末位；引擎子进程已死时允许重新拉起并换模重试。

### 新功能

- **识别预设：Qwen3-ASR Galgame + TEN：** 软声组新增；（推荐搭配，耗时长于Anime Whisper，质量更高）。
- **识别预设：Anime Whisper + FireRed：** 软声 / 动漫实验预设。
- **QC静音切割：** 一键QC修复时（含主窗口的任务完成后QC修复），支持超长字幕时自动进行静音切割（字数长度可设置，设置入口：设置 > 任务处理 > **QC 修复 · 超长句静音分割字数**）
- **ASR 低置信二意见（全版本）：** 批次写入置信度后，对**低置信且高风险**时间窗用已安装的兄弟 ASR 局部重转并择优写回；过短/空窗跳过，单窗不连试 tiny/turbo。改原文后译文档重叠句可清空为「…」。设置 → **任务处理** → **ASR 低置信二意见**（自动 / 始终开启 / 关闭；默认自动，软声或日语 ASR 时生效）。
- **智能翻译多轮加固（Pro）：** 风险抽样润色后增加**忠实复核**与**称呼一致性**轻量第二遍；默认开启。设置 → Pro → 智能翻译。
- **QC 重转写合并低置信窗（Pro）：** 智能修复的局部重转写与低置信 ASR 窗共用配额；复查后可提示对照训练残留。
- **学习向导（Pro）- 实验阶段：** （主窗口更多按钮内）通过对字幕进行智能训练分析学习来提升翻译能力。此功能为实验阶段，会持续完善更新。



### 优化

- **Qwen3-ASR 人声岛加速：** 默认 TEN/FireRed 路径改为内存 PCM 切片 + 批量推理（1.7B 每批 8 段）；显存不足时自动拆批。`qwen3-asr-1.7b-ja-anime-galgame` 等 1.7B 专科会明显快于逐段 transformers 调用。
- **模型库浏览空间过小：** 「设置 → 模型」改为摘要入口 +「打开模型库」大弹层浏览/下载；关闭弹层后下载进度仍可在摘要条看到。
- **更新 llama-server** 到 b10453 版本
- **润色风险抽样扩权（Pro）：** 剧情贴合润色更偏向 QC 怪句/通顺度、ASR 低置信、blank recover 等高风险句。
- **空/省略译优先补译（Pro）：** QC 智能修复的语义审阅优先捞「…」/空译文（含二意见对齐清空条），给出可写回的 suggestedTarget。
- **智能翻译润色可看性（Pro）：** 剧情贴合润色优先捞「嗯」欠译、先輩/人名不齐、明显事实错译；润色不再以「语意不变」挡住补译。
- **后处理少删句：** 成对清理不再因译文被压成「嗯/哈哈」而删掉仍有实词的日文。



## 3.1.1



### BUG修复

- **字幕编辑器播放时列表空白：** 开启自动焦点并播放时，左侧字幕列表向下滚动只剩空白；现已在滚动时持续刷新窗口化列表，无需先暂停
- **进度条临时文件自动清除：** 每次任务完成后自动清理临时文件，避免长时间使用后磁盘空间占用越来越大
- **编辑器 LLM 后，主窗口显存溢出：** 关字幕编辑器不再把本地模型空闲挂起约 5 分钟；末个编辑器关闭时腾出显存



### 新功能

- **繁体中文界面：** 设置 > 常规 > 启动与界面 可切换**简体中文 / 繁体中文**；
- **字幕编辑器 ASS 样式（Pro）：** SRT/VTT 可「转为 ASS」后编辑保存；列表/详情 Style 选择；我的预设包；内置预设；行内颜色/字号/特效；说话人→Style 映射；双语叠层可写入编辑文档或导出；预览区拖字幕点调整 `{\pos}`；详情改特效后强制刷新 JASSUB 预览（播放中可见淡入/模糊等）
- **特效字幕（Pro）：**ASS字幕格式（或在字幕编辑器转换为ASS格式）时支持字幕特效



### 优化

- **字幕编辑器详情区布局：** ASS 控件按「排版 / 样式·位置」分行；对齐与落点改为下拉菜单（不再用九宫格占行）；当前 CPS 并入底部统计，时间行更紧凑
- **临时文件自动清除：** 每次任务完成后自动清理临时文件，避免长时间使用后磁盘空间占用越来越大



### 新增模型

注意：下列新增模型为**实验阶段**，暂为做深入的优化适配，后续会针对部分新增模型进行优化适配，再做进一步说明。

- **ASR：Cohere Transcribe 03-2026** — 本地 2B Conformer（14 语含英/日）；英语表现强；体积较大，请按需下载。**（感谢 emsb8888-prog 的测试和推荐）**该模型可能需手动下载（或在huggingface获得token和授权后在软件端开VPN下载）。
- **ASR：Qwen3-ASR 1.7B JA Anime/Galgame** — `jaykwok` 动漫/Galgame 微调（约 4GB）；
- **ASR：Qwen3-ASR 1.7B JA（neosophie）** — 专有名词/汉字微调（约 4GB）；
- **Qwen3-ASR 时间戳**；
- **VAD：FireRedVAD** — 默认包含在软件包内；



## 3.1.0

本版本改动和新增功能非常多，为避免赘述，以下只列出一小部分。**建议所有用户都用向导进行重新设置。**

遇到问题可通过右上角 **使用反馈** 提交，会尽快得到回复和处理。

### 新功能

- **字幕库：** 可浏览本机已生成的字幕（支持多个版本），支持预览、边看边对照
- **全新的智能翻译（Pro）：** 日语默认先用专训翻译模型出句，再做一轮语气润色贴合；语句更带人味
- **拟声、语气词处理：** 设置 → 任务处理 可清理多余标点、语气词、拟声词
- **新增 任务执行中 添加、删除操作**： 不需要等任务结束后才能对任务列表进行操作了
- **新增 断点继续：** 任务过程中中断，可支持断点继续了
- **新增 失败提示：** 任务失败时，在状态显示错误信息，并给出多种解决方案



### 优化

- **翻译更好选：** 「翻译」合并成一个入口；可自动匹配模型，也可指定已下载的模型。
- **识别预设更好找：** 按清晰对白 / 低显存 / 日语软声 / 影视 / Anime 等场景挑选；套用预设只改识别相关，不再改翻译方式
- **语言优化包：** 打开 Pro 页会自动检查更新；有新版本时直接提示并可一键更新
- **更新Transub Engine引擎**
- **更新llama-server**到b10437版本
- **更新内置词库**



### BUG修复

- 修复 Whisper 等本地安装包解压较久时，被误报「超时失败」的问题
- 修复Pro功能提示缺少闭源模块，导致部分Pro功能丧失的问题
- 修复了向导和系统检查存在的错误检测信息，并调整向导顺序



### 新增模型

- **翻译模型：GalTransl系列  -** Sakura系列模型的分支版本，针对日语软声、动漫、游戏等场景做了专属优化。
- **ASR语音识别模型：Parakeet系列** - NVIDIA 基于 NeMo 框架推出的高性能自动语音识别，推荐英语场景使用。



### 如何生成更好的字幕？（建议）

默认预设是为了适配更多的用户，在质量、速度之间取得一个平衡点。如果要获得更高质量得字幕，需要在各项参数内做更多的调试，同时ASR和翻译模型的选择也至关重要。模型的侧重点、参数量等等都对字幕质量起到至关重要的作用。所以喜欢折腾的朋友们，可以多试试，也欢迎把测试后的数据反馈给我。

1、自定义参数时请关掉智能感知，按片源类型选匹配的识别模型（日语软声可试 Anime Whisper预设）

2、有 Pro 时推荐开智能翻译；模型可试 **Qwen2.5 7B Instruct**（显卡更好可上更大）

3、译文不满意时，也可把 `subtitle` 里的原字幕拿给其它大模型重译，或用 Pro 外接模型（BYOK）

4、非影视片不建议开影视音频增强，这会大幅增加耗时，且非影视剧时质量未必会好。

## 3.0.7

目前版本，Transub已经内置了多个主流模型。但想获得更好的字幕质量，仍需要对各种参数进行微调（**在使用自定义参数时，切记关闭智能感知开关**）。欢迎大家把自己调试认为满意的参数分享到[https://github.com/dlsandy/Transub/issues/new](https://github.com/dlsandy/Transub/issues/new)

**具体分享步骤如下**：设置 > 识别参数 > 保存当前为预设 > 导出预设 （最后将该预设文件分享到github）

我仍然欢迎大家发现bug或功能上的建议可以通过 **使用反馈** 提交，我会尽快回复并修正。

**本次更新属于紧急更新，以修正关键Bug和内置词库更新为主，无新增功能。**

### 优化

- **内置词库更新：** 大量优化了日译中的内置词库



### 如何生成更好的字幕？（建议）

1、避免使用智能感知（智能感知是为了在速度、质量等各方面做了综合考量后的功能），可以试试根据自己的影片类型去处理模型里选择相匹配的模型，各个模型都有简单的介绍。（本次版本更新大量优化了Anime Whisper的语音识别结果，该模型针对日语软声识别特别强，推荐大家可以测试一下）

2、开启智能翻译，翻译过程中会对全片进行理解并整理出影片概要后再进行翻译，使翻译更具人味（需解锁Pro功能），如仍然不满意那就使用理解重构功能。（这里推荐使用智能翻译模型和LLM推理模型设置为：**Qwen2.5 7B Instruct**（当然如果你的硬件配置够好，你可以上更大规模的模型））

3、Transub目前听写的字幕质量已经颇高，如果觉得翻译结果不满意，可以从subtitle文件夹中直接把生成的原字幕文件发给第三方更大的模型进行翻译。当然你也可以使用Pro功能里的外接模型（BYOK）实现高质量的翻译和推理。

## 3.0.6

目前版本，Transub已经内置了多个主流模型。但想获得更好的字幕质量，仍需要对各种参数进行微调（**在使用自定义参数时，切记关闭智能感知开关**）。欢迎大家把自己调试认为满意的参数分享到[https://github.com/dlsandy/Transub/issues/new](https://github.com/dlsandy/Transub/issues/new)

**具体分享步骤如下**：设置 > 识别参数 > 保存当前为预设 > 导出预设 （最后将该预设文件分享到github）

我仍然欢迎大家发现bug或功能上的建议可以通过 **使用反馈** 提交，我会尽快回复并修正。

### 新功能

- **新增 ASR 模型（按需下载）：** Whisper large-v2（`Systran/faster-whisper-large-v2`） / 可自选或在 **设置 > 识别参数 > 预设 > 「日语 · large-v2 翻译（可选）」** 快速匹配参数。
- **内置预设：** 新增「日语 · large-v2 翻译（可选）」



### 优化

- **模型手动下载：** 针对模型下载难题增加了手动下载的向导操作（设置 > 处理模型 > 对应模型的手动下载按钮）
- **内置预设优化：**「日语 · Anime Whisper 翻译」现在默认开启 （完成后精简纯语气词）。
- **完成后精简纯语气词：** 加强了杂音、幻听、无意义的语气词的清洗过滤。



## 3.0.5

此次更新的内容较多，如使用过程中遇到bug，可以通过软件界面右上角 **使用反馈** 进行提交。提交的问题会被尽快修复！

此次更新推荐试用：Anime-Whisper（主窗口AW按钮开启，针对日语软声/动漫/游戏特别优化）

### BUG修复

- **深色主题Bug：** 在深色主题时关闭字幕编辑器会强制退出应用的问题
- **双语字幕：** 修复3.0.4版本存在的双语字幕未合并的bug
- **托盘恢复空白：** 任务进行中最小化到托盘、任务完成后再打开主窗口内容空白（仅剩标题栏/背景色）



### 新功能

- **重新翻译：** 新增的重新翻译功能，可对字幕选择更多的模型进行再次翻译
- **理解重构：** 任务列表「理解重构 ◆」打开字幕编辑器并启动影片理解重构（片名/简介/强度、Brief、对照确认与编辑器一致）；一次仅支持勾选一部影片
- **任务列表右键菜单：** 在任务行上右键可编辑字幕、重新翻译、理解重构、一键 / 智能修复 QC、重试、在文件夹中显示、感知采纳 / 深度感知、从列表移除
- **精简纯语气词：** 开启后将自动清理 纯语气/拟声 词（**默认不开启**）**（入口：设置 > 任务处理 > 完成后精简纯语气词）**
- **任务列表：** 可以点击表头进行排序了，表头支持右键菜单自适应调整宽度



### 优化

- **主窗口也支持深色主题了：**（设置 > 常规 > 界面主题）
- **精简纯语气词加强：** 识别带顿号的喘息/轻笑（如「はぁ、はぁ」「ふふっ」「うん、うん」）与装饰符（♡），空白原文槽也按纯语气成对删除；仍保留有语义对白（いいえ / 好的 / 句末语气词等）
- 针对Anime-Whisper的ASR语音识别模型进行了深度优化，使听写日语软声字幕质量进一步提升
- 加强了Sakura 翻译模型的翻译质量
- 加强了影片理解重构的能力：预览可编辑人物/专名/主线/避免项；



### 如何生成更好的字幕？（建议）

1、避免使用智能感知（智能感知是为了在速度、质量等各方面做了综合考量后的功能），可以试试根据自己的影片类型去处理模型里选择相匹配的模型，各个模型都有简单的介绍。（本次版本更新大量优化了Anime Whisper的语音识别结果，该模型针对日语软声识别特别强，推荐大家可以测试一下）

2、开启智能翻译，翻译过程中会对全片进行理解并整理出影片概要后再进行翻译，使翻译更具人味（需解锁Pro功能），如仍然不满意那就使用理解重构功能。（这里推荐使用智能翻译模型和LLM推理模型设置为：**Qwen2.5 7B Instruct**（当然如果你的硬件配置够好，你可以上更大规模的模型））

3、Transub目前听写的字幕质量已经颇高，如果觉得翻译结果不满意，可以从subtitle文件夹中直接把生成的原字幕文件发给第三方更大的模型进行翻译。当然你也可以使用Pro功能里的外接模型（BYOK）实现高质量的翻译和推理。

## 3.0.4



### BUG修复

- 修复字幕编辑器切换深色主题后关闭时，主窗口被一并关闭并导致应用强制退出的问题（`nativeTheme` 仅在用户切换主题时更新，关窗不再回切）
- 修复任务进行中最小化到托盘再打开时，主窗口内容空白（仅剩标题栏/背景色）的问题
- 修复最小化到托盘后息屏 / 解锁，再从托盘打开时主窗口内容空白的问题
- **托盘空白根因修复（Windows）：** 最小化到托盘改为 soft-park（透明度 + 移出屏幕，不再 `hide()`），避免 Chromium FrameEvictor 丢掉画面；恢复后若仍空白会自动 reload 一次渲染进程。窗口管理拆分为 `window-state` / `main-window-tray`
- 修复 3.0.3 智能感知误推未安装的 Whisper Large v3 Turbo：影视等场景改回 SenseVoice；日语软声优先日语专用 Whisper
- 修复灵敏检出（WhisperSeg）过稀时中间大段对白丢失的问题（覆盖率过低会回退更稳的切分）
- 修复部分网络环境下 Pro 语言优化包（TDP）无法检查 / 下载更新的问题
- 修复安装 WhisperSeg GPU 组件时与 CPU 版 onnxruntime 冲突，导致灵敏检出仍不可用的问题
- 修复引擎路径双语任务勾选「合并双语」后未写出与影片同名合并字幕的问题（此前仅在勾选「合并后删除单语」时才合并 SRT，且依赖 `*.dual.ass`）



### 新功能

- **应用级统一深色主题：** 主窗口、字幕编辑器、设置与 Electron 标题栏共用同一深浅色偏好；设置「界面主题」移至常规 → 启动与界面；主窗口文字 / 按钮 / 菜单 / 任务表 / 设置页对比度已按深色适配
- **使用反馈：** 主窗口工具栏新增「使用反馈」，可打开 GitHub Issues 提交 Bug 或建议
- **重新翻译：** 主窗口参数行右侧新增「重新翻译」；对已勾选项检查转录字幕（原文轨 / 转录缓存），弹窗选择机器 / 推理 / 智能翻译模型后跳过 ASR 直接重译
- **新增 ASR 模型（按需下载）：**
  - **Anime Whisper** — 动画 / Galgame 日语演技更稳，软声场景更合适
  - **Kotoba Whisper v2.0** — 日语蒸馏模型，通用日语、段级时间戳
  - **ReazonSpeech K2** — 日语 Zipformer，自带较细时间戳
  - **Qwen3-ASR 0.6B** — 多语识别
- **内置预设：** 新增「日语 · Anime Whisper 翻译（可选）」



### 优化

- **Anime Whisper TEN 路径：** 复用 VAD 阶段已解码的 16 kHz PCM，逐帧直接切片送入缓存 Whisper 模型，去掉每帧 ffmpeg 重解码与磁盘 WAV（时间轴仍由 VAD 帧边界决定）
- **Anime Whisper TEN 推理：** 岛路径强制 `beam_size=1`（greedy，对齐 litagin / WhisperJAV）；预设同步为 1，减少短帧 beam search 开销
- **Anime Whisper 听写（零加时）：** 同步并扩充 `ja-asr-domain-fixes`（软 AV / 动画常见误听：おっぱい・ビキニ・気持ちいい・バイト等）；仍禁 prompt；不默认 Demucs/双模型/加长帧；内置 TDP 更新至 **1.0.7**（D01 含 adult ASR）
- **精简纯语气词（可选）：** 设置 → 任务处理 →「完成后精简纯语气词（成对删除）」；开启后日中两侧均为纯语气/拟声的条目成对删除并重编号；默认关闭
- **杂音清理改为真删除：** 不再把译文杂音置空为「…」；有原文+译文且条数一致时，从两侧成对删除以保持 JA↔ZH 对齐
- **日译中提示词：** Sakura / 忠实语气补充「纯拟声可短译、有语义须译全句、勿压成单字语气词」；QC 智能修复提示保留句末语气词



## 3.0.3



### 升级提示

- 本版本修改内容较多，建议全量下载并覆盖旧版本后，再次进入 设置 > 向导 > 按向导流程完成新版本的设置
- Pro用户请重新再Pro页面进行激活



### BUG修复

- 一键修复解压运行库时不再把整个软件卡死，修复过程中仍可重新检测
- 打开设置、编辑器等窗口时，主窗口最小化后不再马上弹回来
- 任务进行中最小化到托盘再打开，主窗口不再卡住点不动
- 主窗口在托盘时关闭设置窗口（如下载过程中），再从托盘打开不再出现空白内容
- 反复打开关闭设置后，主窗口不会越开越大
- 修正目标为繁体中文时，转换文字错乱的bug。
- 超长视频（如数小时）用 Whisper 转写时，不再容易在进度中间因内存不足失败
- 显卡驱动已是较新 CUDA，本机却装着稍旧 GPU 运行库时，系统检查不再误报 WhisperSeg GPU 未就绪；一键修复也能正常消除相关警告



### 新功能

- **任务完成后自动修复QC：** 可在 主界面 开启任务完成后自动修复QC问题；
- **繁体中文字幕：** 支持更多的繁体中文类型（台湾、台湾字形、香港）
- **Pro语言优化包：** 可在 Pro 页单独检查并更新领域用语数据（不必等软件整版升级）；
- **Pro版QC质检修复：** 可对仍不通顺或连成一片的字幕用大模型润色；主窗口、编辑器、工作流均可使用
- **应用内增量更新：** 检查更新时尽量只下载有变化的部分，体积更小、更快；失败时会自动改下完整 zip。本版本起不再提供安装程序（Setup），请使用 zip 解压版；旧安装版请改下 zip（见下方说明）
- **下载包：** 仅发布 zip（`Transub-*-win.zip`）；CPU/CUDA 完整首装包不再提供，运行库请按向导或下载中心按需安装。日常升级仍走该包，本机已装的模型与运行库会保留



### 优化

- 系统托盘右键菜单可直接打开字幕编辑器、设置
- 向导检测到 CUDA 12 / 13 时，会自动匹配对应的本地大模型运行环境，一键修复与下载语言模型也会装对版本
- 开启「智能感知强制采纳」后，会始终按感知方案跑任务；缺模型时需先安装，不再出现「仍用现有继续」导致误用偏弱配置
- **字幕质检更准：** 带标点的连续中文能正确识别为可分割；叠词与通顺度问题去重；单独缺标点不再刷一堆通顺度；摘要会区分「可自动修」与「建议人工」
- **一键规则修复更全：** 主窗口同步处理叠词、杂音、连续重复、无效时间轴等；编辑器与工作流默认更积极清理叠词与杂音
- **智能修复（Pro）：** 规则修完后，可对仍不通顺或连成一片的字幕用大模型润色；主窗口、编辑器、工作流均可使用
- **局部重转写（Pro）：** 对问题片段可结合视频局部重新转写（需关联视频），不必整片重来
- **智能断句（Pro）：** 对读速过高或过长的句子自动找断点并重新分时
- **词库升级（Pro）**： 已更新智能翻译词库到最新版本
- **按内容类型调参：** 影视 / 访谈等素材会自动调整语速阈值、清杂音强度以及是否做局部重转写与润色
- **双语语义审阅联动（Pro）：** 智能处理后可对问题条做语义审阅，并一键采纳建议译文；双语/翻译任务会自动带上原文对照
- 智能处理时有等待提示与阶段说明；编辑器里高级项默认不勾选，需要时再开
- 编辑器一键规则修复写回前会弹出对照列表，可勾选或改完再确认



### 其它



#### 如何选择下载包（首装）

本版本起**不再提供安装程序**。请从 [Releases](https://github.com/dlsandy/Transub/releases)（或 Codeberg 镜像）下载 zip，解压后运行 `Transub.exe`。


| 包        | 文件名示例                      | 说明                              |
| -------- | -------------------------- | ------------------------------- |
| **发行包**  | `Transub-3.0.3-win.zip`    | 首装与应用内完整升级用包；模型与运行库按向导或下载中心按需安装 |
| **升级合集** | `Transub-3.0.3-update.zip` | 增量/区块更新合集（非直接解压）                |


选型提示：

- 用户下载请认 `Transub-*-win.zip`
- 首次打开请走设置向导补齐运行库 / 模型；体积可能较大，请耐心等待



#### 旧版本如何升级到 3.0.3

1. **已经是 zip 解压版**
  - 打开软件 → 设置 → 更多 → **检查更新**（或开启「自动检查更新」）  
  - 一般会尽量只下载有变化的部分；必要时再下完整 zip（`Transub-*-win.zip`）  
  - 主程序退出后会出现安装进度窗口，请勿强制结束  
  - 已下载的运行库、模型、本地大模型等会保留
2. **仍在使用旧安装程序版**
  - 安装程序版已停止更新，应用内更新无法继续使用  
  - 请到 Releases 下载 zip（`Transub-*-win.zip`），解压到新目录运行  
  - 配置与许可一般仍在用户数据目录，多数情况可直接沿用；若向导提示缺运行库，再按需下载  
  - 确认 zip 版可用后，可自行卸载旧安装（可选）
3. **手动覆盖升级（可选）**
  - 下载 `Transub-*-win.zip`，解压覆盖原目录（或解压到新目录后改快捷方式）  
  - 请勿删除目录里已装好的模型、GPU 包、本地大模型等数据
4. **Codeberg 镜像**
  - 与 GitHub 同步发版。应用内更新会自动选较快的下载线路



## 3.0.2



### BUG修复

- 修复字幕编辑器最小化（或关闭）后，主窗口内容变空白的问题
- 修复字幕编辑器无法打开 ASS / SSA 字幕文件的问题（支持导入编辑并保留样式头）
- 修复智能翻译过程中引擎超时重试撞上「adapter busy / HTTP 429」导致整单失败的问题（适配器排队 + 引擎忙等待重试；智能翻译 HTTP 超时延长至 30 分钟）
- 修复任务失败 / 取消 / 异常中断后 `llama-server` 与引擎进程常驻后台的问题（批次结束回收本地 LLM；失败或中断时同时停止引擎；关闭窗口时强制回收）
- 修复译中把完整对白软省略成「嗯， / 啊 / 请」等语气词后仍视为有效译文的问题（欠译判定；推理翻译对可疑/空白条逐条补译；智能翻译经适配器清洗）
- 修复合并双语 ASS 时译文/原文上下行顺序与设置不一致的问题



### 新功能

- **llama-server 可选 CUDA 后端：** 设置 → 运行环境可在 Vulkan与 CUDA 12（NVIDIA）（默认）之间切换；
- **应用内更新：** 同时检查 GitHub / Codeberg；下载 zip 前测速选较快线路；主程序退出后显示独立安装进度窗口



### 优化

- 扩充日语 ASR 域纠错词表；
- 加强译后清洗（叠名 / 敬语误译、提示词泄漏、跨条 stuck 译文、软省略欠译等）；人名术语 harvest 更保守
- WhisperSeg 等按本机 CUDA 12 / 13 自动匹配 onnxruntime-gpu，降低灵敏检出掉到 CPU 的概率
- 模型 / 运行时 / 应用更新下载支持多镜像测速
- **感知交互：** 任务列表感知徽章可点击切换采纳 / 不采纳；「重感知」改称「深度感知」



### 关于Pro版本

Pro 主要加强本地大模型智能化（智能翻译、语境 / 影片理解重构、语义审阅、智能修复、Demucs 等），对 **内存 / 显存** 要求明显高于免费功能。

免费版的转写、Opus、推理翻译与字幕编辑器，已能覆盖多数日常需求且精度很高。没有硬性 Pro 场景时，**请谨慎开通**；硬件不达标时开通后也可能跑不动或极慢，**开通后不支持退款**。

#### 硬件配置分档（对照选购）


| 档位     | 建议硬件                                          | 建议模型                     | 效果预期                                |
| ------ | --------------------------------------------- | ------------------------ | ----------------------------------- |
| **入门** | 内存 ≥8 GB；核显或约 4 GB 独显                         | Qwen 1.5B～3B、Sakura 1.5B | 轻度改写可用；智能翻译偏慢，仅适合短片                 |
| **推荐** | 内存 ≥16 GB；NVIDIA **6～8 GB** 显存（或同级 Vulkan 独显） | **Qwen2.5 7B**、Sakura 7B | 日常 Pro 全流程可用（翻译 / 重构 / 审阅 / Demucs） |
| **进阶** | 内存 ≥32 GB 或 **12～16 GB** 显存                   | Qwen **14B** 级           | 成片一致性与理解更好                          |
| **旗舰** | **24 GB+** 显存                                 | Qwen **32B** / 30B-A3B 等 | 本地最高质量，耗时长、更吃散热与电源                  |


- 可用 **云端 BYOK** 降低对本地显卡的依赖（仍建议内存 ≥8 GB）。
- Demucs 人声分离优先 NVIDIA CUDA；纯 CPU 通常过慢。
- 详见 README「Pro 硬件配置分档」。



### 其它

- **推荐安装**：下载 `Transub-3.0.2-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-3.0.2.exe`
- **升级**：建议使用软件内检查更新完成升级（无需重复下载运行环境和模型）



## 3.0.1



### BUG修复

- 修复智能感知错误识匹配日语影片翻译模型的bug
- 执行新任务时会自动清空任务列表非此批任务的bug



### 新功能

- **快捷一键修复：** 生成任务执行完成后，可点击一键修复QC按钮将列表内存在QC问题的字幕一键修复，或通过列表内单个字幕后面的扳手图标一键修复QC
- 增加了自动升级检测功能（入口在设置 > 更多 > **自动检查更新**）
- **本地大模型手动放置校验：** 校验 GGUF 文件头与体积，识别下错/下残文件；目录内发现近似文件时提示正确文件名；设置页对异常文件显示「文件异常」
- **单文件 GGUF 手动下载：** 大模型目录与处理模型卡片增加「手动下载」；说明文件名与应放置目录（`advanced-llm/models`），支持打开下载链接 / 打开目录 / 放入后检测
- **智能感知加强：** 扩大文件名识别范围（动画/综艺等）；快速感知结果为「未识别」时自动加深一次（短窗语种 + 声学），并据声学/片长证据提升类型匹配



### 其它

- **升级**：建议使用软件内检查更新功能完成升级（无需重复下载运行环境和模型）
- **推荐安装**：下载 `Transub-3.0.1-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-3.0.1.exe`

**注意：如需继续使用TransWithAI**外部引擎，请下载2.0.0版本（3.0版本不再支持该引擎）

## 3.0.0

**（此版本增加和修改的功能和项目非常多，难免存在BUG，欢迎发帖告知，我会在第一时间进行修复优化）**

重大升级版本：默认处理引擎改为内置 **Transub Engine，新引擎极大的提升了转录和翻译质量并支持大模型接入。 全新的智能感知功能，自动识别拖入的音视频并自动匹配内置感知参数，使用起来更简单，效果更好。**全面优化和增强Transub Editor字幕编辑器功能，提供了诸多全新的功能，并大幅减轻长片编辑卡顿。且带来了自定义布局功能，适应各种不同喜好的字幕编辑布局习惯。

**首次使用建议使用向导完成系统检查和设置。**

产品对比表见 [README.md](README.md#compare-2-vs-3)。产品边界见 [NOTICE](NOTICE)、[LICENSE-PRO](LICENSE-PRO)。

### 相对 2.0：功能速览


| 主题    | 2.0            | 3.0                                                      |
| ----- | -------------- | -------------------------------------------------------- |
| 默认引擎  | 外装 TransWithAI | **内置 Transub Engine**                                    |
| 素材配参  | 开始前可选匹配        | **按项自动感知**（类型即感 / 语种 / 声学 / 纠错记忆）                        |
| 免费译中  | 依赖已有路径，以日译中为主  | **机器翻译 + 推理翻译（支持多语言）**；忠实语气                              |
| 智能翻译  | —              | **Pro 专属**（影片简要理解 → 分块译 → 保持一致性）                         |
| Pro   | —              | 语境/影片重构、影视音频增强、语义审阅、BYOK/大模型、ASS 说话人样式；**大版本内买断**（爱发电领取） |
| 编辑器   | 双语 / 预设 / 工作流  | + 工作区模式、书签/A-B、说话人与审校、原文缓存、导出清单、规则审阅等等数十项加强              |
| 下载与更新 | 分散             | **统一下载中心**；zip/NSIS **应用内更新**（自动保留已下载的 支持库、模型、推理文件）      |




### 相对 2.0：性能与体验


| 维度      | 3.0 改进                                   |
| ------- | ---------------------------------------- |
| 拖入感知    | 文件名快感优先；快速匹配，深入探测按需「重感」                  |
| 长片列表    | ≥180 条字幕局部加载；降低性能占用，减少卡顿                 |
| QC / 查找 | 长片改走 **Web Worker**                      |
| 撤销      | **Undo 补丁**相对基线，降低内存占用                   |
| 显存      | ASR→MT 前释放；本地 LLM **空闲约 5 分钟停服**，避免重构后卡顿 |
| 生成器界面   | 4K **界面缩放**；任务资源占用条（CPU/内存/GPU）          |
| 安装设置    | 向导式安装，减少复杂的参数设置                          |
| 启动      | 可设置默认启动项为编辑器或生成器                         |




### 新功能



#### 引擎与生成

- **内置 Transub Engine 引擎**
- **按项自动感知**：拖入后分析类型（情景剧 / 影视 / 访谈）、语种、ASR·MT、VAD/增强；有结果默认采纳；缺项可引导安装
- **类型即感 / 深入重感**：拖入只做文件名快感；匹配度强立即采纳；「重感知」才跑语种 / 声学 / 纠错记忆（设置中可开启默认重感知）
- **短窗语种探测**（引擎 `detect-language`）+ **声学轻探测** + **感知纠错记忆**（同目录偏好）
- **翻译三选一**：引擎 Opus（多语→中）/ **推理翻译（Sakura 等，免费）** / **智能翻译（Pro）**
- **智能翻译（Pro）**：影片简要 → 分块译 → 一致性；经引擎 `mtBackend=external`；可与 LLM 推理分用途选模型
- **影视音频增强（Pro）**：Demucs 人声分离 + 电影级 VAD；免费侧保留更激进切分 / 轻度降噪
- **灵敏检出**（WhisperSeg）、幻觉静音阈值等
- **译后清洗**：叠名幻觉、句末臆造人名、术语统一、提示词回声等（引擎 + 桌面双层）
- 字幕默认同名写出（不再强制 `.zh.srt`）；引擎支持 ASS / 合并双语 `*.dual.ass`
- **统一下载中心**（模型 / GPU / Demucs / GGUF / 应用更新）、**设置向导**（独立窗口）、任务资源占用条、界面缩放（4K）
- **zip / NSIS 应用内更新**：保留 `transub-engine/models`、`advanced-llm` 与已装 GPU / Demucs 等 site-packages



#### 字幕编辑器

- 工作区：**精修 / 时间轴 / 双语 / AI / Pro**（`Alt+1…5`）；模式工具条直接露出
- **书签 / A-B 循环**、说话人与审校状态（未看/已改/已通过）、建议说话人
- **原文缓存**：挂副轨、Diff、选区/全文恢复误删；重构/翻译可注入 source
- **导出前检查清单**、双语规则审阅、**双语语义审阅（Pro）**
- 列表窗口化（≥180）、Undo 补丁、QC/查找 Worker、时间轴磁吸、真 ASR 置信度写入 `.transub.json`
- **语境重构 / 影片理解重构（Pro）**：Film Brief 预览、对照确认、失败块重试、书签段工作流
- ASS 说话人样式导出（Pro）；情境条快捷 Pro 动作



#### Pro 与许可

- **大版本内买断**（**硬限制 1 台设备**、30 天换机；覆盖本主版本如 3.x）；[爱发电](https://afdian.com/a/transub) 订单号「领取并激活」或粘贴 `TSUB1.…` 密钥；设备绑定由 Cloudflare Worker 联网强制
- 大模型：BYOK 或软件内 GGUF + llama-server（Vulkan）
- 许可说明见 `LICENSE` / `LICENSE-PRO` / `NOTICE`



#### 特别感谢

网友huihuil提供大量宝贵的意见和大量测试，使得软件功能更完善和更少的bug。再次表示感谢！

### 其它

- **推荐安装**：下载 `Transub-3.0.0-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-3.0.0.exe`



## 2.0.0



### 重大更新

- 软件架构进行了大量的优化，使性能更进一步提升。
- 重构后的界面，将常用操作集成在主窗口内，使操作更便捷。
- 增加大量实用新功能（双语字幕、字幕预设、工作流等）。



### 新功能

- **双语任务**：单次任务即可输出原文和译文，可选合并输出双语字幕；（双语功能需分别下载和设置转写/翻译模型）
- **导出繁体中文字幕**：主界面设置目标为繁体中文时，可生成繁体中文字幕
- **独立的字幕编辑器启动项**：可通过Transub Editor图标快速启动字幕编辑器，快速查看/编辑历史字幕文件
- **字幕预设**：编辑器支持按时段批量插入片名/演员/字幕作者等预设文案
- **工作流**： 创建你自己的工作流，将重复流程用工作流快速批处理。（已内置两套常用工作流）
- **双语字幕编辑**： 支持双语字幕编辑，可设置原文和译文的上下位置，支持双语字幕合并。
- **任务历史：** 支持历史任务的查看、编辑和清除



### 性能 / 启动体验

- 优化了软件冷启动速度
- 优化了任务文案，更好的理解任务状态
- 优化了字幕编辑器波段操作时的卡顿现象
- 重构了主界面UI，各项操作、信息更一目了然。
- 重构了设置界面，分为标准和专家两种设置模式。新手不必再为看到一堆复杂的设置项头痛了。
- 配置预设，可将各项设置项进行存储，便于各种不同情况下快速切换配置项。



### 其它

- **推荐安装**：下载 `Transub-2.0.0-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-2.0.0.exe`



## 1.4.4



### 修复

- **Smart App Control 拦截**：关闭 electron-builder 向 exe 写入 asar integrity（该改写会让未签名的 `Transub.exe` 在 Windows 11「智能应用控制」强制模式下被拦截）；仍保持未签名，图标/版本信息继续由 after-pack 写入



### 其它

- **推荐安装**：下载 `Transub-1.4.4-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.4.exe`



## 1.4.3



### 修复

- **打包缺词典**：`subtitle-chinese-core` 依赖的 `subtitle-chinese-dict` 未打入 `app.asar`，简繁转换仍会失败；打包清单改为纳入全部 `*-core.js` 与该词典



### 其它

- **推荐安装**：下载 `Transub-1.4.3-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.3.exe`



## 1.4.2



### 修复

- **打包缺模块**：正式包 `app.asar` 未打入 `subtitle-chinese-core`，批量完成后做简繁转换时报 `Cannot find module '../src/js/subtitle-chinese-core'`



### 其它

- **推荐安装**：下载 `Transub-1.4.2-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.2.exe`



## 1.4.1



### 新功能

- **压缩叠词**：将连续重复 ≥ 3 次的短语/单字压成「词…词！」（如「好的」×5 →「好的…好的！」），降低 CPS；编辑器工具菜单、当前条目工具栏、QC 一键修复均可用



### 设置

- **完成后压缩叠词**：设置 → 编辑 → 批量完成后；默认开启，写回字幕
- **完成后自动按 CPS 拆句**：默认改为开启（此前默认关闭）



### 字幕编辑器

- **列表全文显示**：字幕列表文本不再两行截断，长句可完整换行查看
- **QC 叠词检测**：质量检测可标出叠词嫌疑，一键修复可选压缩叠词



### 其它

- **推荐安装**：下载 `Transub-1.4.1-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.1.exe`



## 1.4.0



### 题外话

- **更新频率**：自项目开始至今几乎保持着每天更新的节奏，但目前各项功能都已较为完善且稳定。**后续会大幅度的降低更新频率**。
- **新功能**：目前各项功能已经非常强大，尤其是字幕编辑器的功能已经强于大部分同类字幕编辑器。目前软件所呈现出来的功能已经是我所能想到的几乎所有想法了。**所以对于新功能，更多的是需要依靠网友们的想法和建议了。**
- **MAC版本：**暂无MAC版本的开发计划（有开发MAC版本的开发者可借助项目开源代码只需微做适配即可制作出MAC版本）



### 新功能

- **波形编辑器：支持放大缩小波形尺寸，通过拖动、拉拽等方式快速调整字幕时长**
- 全局术语表：在翻译模式下，自动应用全局术语表（在设置内开启，默认关闭）
- 抑幻听：通过设置，可极大程度减少幻听、杂音、错误转录BGM等情况
- 自动拆句：对于单条过长的字幕按语句自动拆分（需在设置中 开启 **自动按CPS拆句**）
- 生成繁体字幕：现在可以直接生成繁体中文字幕了（在设置的输出项内的中文字幕类型，选择 繁体中文）



### 主窗口

- **独立设置窗口**：设置改为独立 BrowserWindow；从字幕编辑器打开设置时不再顺带唤起主任务窗口



### 字幕编辑器

- **波形默认开启**：打开编辑器时默认显示波形时间轴（仍可关闭；本地已保存的开关偏好优先）
- **时间轴/波形右键菜单**：在时间轴字幕块与波形对应区间上右键，可打开与字幕列表相同的上下文菜单（分割、对齐、时长调节等）
- **删除杂音**：新增「幻觉短句」规则（完毕 / ○○○ / 过短占位等）



### 设置

- **设置分区重排**：8 个页签收为 6 个——**常规**（预设/任务/设备/托盘）、**环境**（TransWithAI + FFmpeg）、**输出**、**转录**、**编辑**（编辑器 + 批量完成后）、**更多**；各区分组与说明文案更清晰，打开设置默认进入「常规」
- **中文字幕类型**：输出页可选简体中文 / 繁体中文（默认简体）；仅「翻译」任务生效，完成后用 OpenCC 做简繁转换（不再写入 initial_prompt，避免「简体中文」等提示被复述进字幕）；独立设置窗口保存后会同步到主窗口，后处理以磁盘配置为准，避免仍按旧的「简体」转换
- **抑幻听参数**：转录页可调无语音/对数概率/压缩比阈值，以及可选的幻听静音跳过；写入 TransWithAI `generation_config`，便于 BGM/静音较多时减少胡编字幕
- **内置预设「翻译 · 抑幻听」**：日语锁定 + 更严的无语音/压缩比阈值 + 较短合并上限（8s）与智能切分 20s，适合 BGM 或静音较多的内容
- **完成后自动按 CPS 拆句 / 清理杂音幻觉**：设置 → 编辑 → 批量完成后；可选写回字幕，缓解单条过长与「完毕」类短句
- **翻译句读后空格**：翻译任务完成后，在 CPS 拆句之前自动在中文 `。？！` 后补空格（已有空白/行末不重复），便于拆句识别句界
- **术语表写入提示**：默认将全局术语写入 `initial_prompt` / `hotwords`（generation_config）
- **参数试跑对比**：设置 → 更多；对勾选视频片头用两套预设各跑一遍并对比 QC



### 平台与分发

- **发行包调整**：处于 portable 版本的运行体验较差，故后续版本仅提供 `Transub-*-win.zip` 与 `Transub-Setup-*.exe`，不再提供 portable 便携版



### 其它

- **推荐安装**：下载 `Transub-1.4.0-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.0.exe`



## 1.3.2



### 平台与分发

- **打包资源封入 asar**：HTML / JS / CSS 不再解压到 exe 旁明文目录，改为打入 `app.asar`，降低被随意篡改的风险（`_internal` 等原生依赖仍保持旁路）
- **默认分发改为 zip + 便携版**：未签名时 NSIS `Setup.exe` 易被 SmartScreen / 杀软拦截；Release 主推 `Transub-*-win.zip`（解压即用）与 `Transub-*-portable.exe`。NSIS 安装包仅作可选本地构建（`npm run build:setup`），不再作为默认发布产物



### 主窗口

- **记住窗口尺寸**：关闭后再次打开恢复上次宽高、位置与最大化状态
- **新版本检查**：更加方便的检查软件新版本，快速用上新功能和bug修复



### 设置

- **默认参数调整**：与常用配置对齐——字幕格式默认仅 SRT；合并间隔 500ms / 最大时长 10s；默认不生成 .bak；托盘进度默认关；完成后 QC 检测默认开
- **系统托盘通知开关**：运行页可开关桌面通知（任务完成 / 最小化提示），默认关闭
- **检查新版本入口**：主窗口设置按钮旁增加图标，可直接检查更新



### 字幕编辑器

- **时间轴/波形可缩放平移**：默认 5× 显示；Ctrl+滚轮或按钮放大缩小，滚轮/滑条左右滚动；倍率写入本地偏好，下次自动恢复
- **新增时间轴/波形区字幕拖动调整时长**： 调整字幕时长更方便了
- **波形加载提示**：开启波形时显示进度遮罩与状态栏提示，避免长视频抽峰时界面无反馈
- **.bak 备份位置可配置**：设置 → 字幕编辑器；可选不生成（默认）、与字幕同目录、或软件数据目录下的 `backup` 文件夹
- **静音分割更精准**：粗定位 + `atrim` 精确取窗（避免关键帧偏移）；单声道高通预处理；切分点按「靠近文本断点 + 静音时长」评分；相邻条目按静音边缘分开
- **静音分割对齐断句词**：理想切点合并空格/换行、断句词与标点；连续无空格文本若匹配断句词也可静音分割



### 修复

- **确认框屏闪**：关闭确认/取消后不再 `blur` 再抢焦，避免短暂露出下层主程序窗口
- **任务栏图标**：开发模式 (`npm start`) 会把 Transub 图标写入 `electron.exe`，避免重装 Electron 后又变回默认图标；从托盘恢复时也会重新应用窗口图标；最大化改在 show 之后，避免任务栏显示宿主 exe 图标



### 其它

- **推荐安装**：下载 `Transub-1.3.2-win.zip` 解压后运行 `Transub.exe`，或使用便携版 `Transub-1.3.2-portable.exe`（均未签名，一般不会像 NSIS Setup 那样被系统拦截）



## 1.3.1



### 主窗口

- **托盘进度与 ETA**：任务进行中更新托盘提示；进度条文案显示已用时间与预计剩余（基于片长与历史吞吐）
- **托盘相关设置**：运行页可开关「任务时更新托盘进度」「开始时最小化到托盘」
- **完成后 QC 检测**（默认关）：设置 → 字幕编辑器；任务结束后仅扫描并标出问题数，不自动修复、不自动打开编辑器
- **启动加速**：窗口 `ready-to-show`、桥接按需加载；FFmpeg / TransWithAI / GPU 检测改为后台快速检查
- **任务队列**：并行探测视频；列表增量刷新；排队完成后确认是否继续
- **设置分区**：原「高级」改为「FFmpeg」；新增「更多」（导入/导出配置、检查更新、任务历史、访问官网）
- **检查更新**：安装包将主进程共用的 `src/js/*-core` 打入 asar，修复「检查软件更新」因缺少 `subtitle-glossary-core` 报错；下载进度条显示
- **取消任务**：Windows 下 `taskkill /T` 结束 infer 进程树
- **任务完成后**：可打开输出目录、播放提示音；GPU 建议可一键应用设备



### 字幕编辑器

- **多选批量**：Ctrl/Shift 点击多选；支持批量删除、合并相邻、简繁/偏移作用于选中；`Ctrl+A` 全选可见行
- **长任务可取消**：静音批处理 / 贴边 / 重转写进度条增加取消；Esc 可中止；FFmpeg 进程可终止
- **可选波形时间轴**：工具栏开关（默认关）；开启后从关联视频抽峰绘制在时间轴下方
- **自动备份与草稿恢复**：保存时写 `.bak`；脏状态定时草稿；重新打开时若草稿更新可选择恢复
- **项目术语表**：字幕旁 `{stem}.glossary.json` sidecar；编辑器术语表弹窗可切换全局/项目，扫描与统一使用合并后的有效术语表
- **简繁短语转换**：OpenCC 短语优先 + 字符映射；可选保护术语表词条（默认开启）
- **批量简繁转换**：工具菜单 → 文本 →「简繁转换」，支持简→繁 / 繁→简，可作用于全部或当前选中条目（可撤销）
- **Ctrl+↑/↓**：文字编辑框内切换上一条 / 下一条字幕
- **快捷键**：工具栏独立按钮（设置后方）



### 修复

- **设置不持久**：打包后用户设置改为写入稳定目录（NSIS → Electron `userData`；便携版 → `PORTABLE_EXECUTABLE_DIR`），不再写到每次解压的临时 exe 旁；并自动迁移旧位置的 `transub-settings.json`（感谢QQ804218提供的bug信息）
- **FFmpeg 目录“消失”**：自定义路径若落在软件安装/`_internal` 内，更新或重装会覆盖该树；现会提示改用安装目录外的路径，且检测保存失败时不再静默忽略
- **查找替换**：全部替换成功后显示「已成功替换 N 处」，不再误报「未找到匹配项」
- **确认框失焦**：确认/取消后恢复编辑器输入焦点（避免需切窗才能继续编辑）
- **任务栏图标**：开发与打包路径正确使用 Transub `app.ico`
- **日志滚动**：转录过程中，日志自动滚动至最新条目（感谢QQ804218的建议）



### 平台与分发

- **GitHub 应用更新**：设置页「检查更新」对接 GitHub Releases
  - NSIS 安装版：若 Release 含 `latest.yml`，可应用内下载并重启安装（electron-updater）
  - 便携版 / 开发模式 / 无 `latest.yml`：通过 GitHub API 比对版本并打开下载页



## 1.3.0

- 字幕编辑器：QC、通顺度、术语表、时间轴、静音/智能分割、局部重转写等
- 内置 FFmpeg；Windows 一键打包脚本



## 1.2.0

- 多种分割模式；共享分割核心；静音检测



## 1.0.x

- 初始基于 TransWithAI 的批量字幕生成与基础编辑

