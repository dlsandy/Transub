# Changelog

## Unreleased

### 新功能

- **原文缓存利用**：生成时保留的转录原文（未清理前）可在编辑器状态栏发现；支持挂为副轨、Diff 对照、按选区/全文恢复误删条目；清理时跳过编辑器正在使用的 soft-pin
- **书签与 A-B 循环**：播放头书签（`B`）、A/B 入出点（`[` / `]`）；状态栏可跳转书签 / 暂停清除循环
- **说话人与审校状态**：手动说话人标签、未看/已改/已通过；情境操作条可快捷标记
- **导出前检查清单**：重叠、空字幕、CPS、低置信、视频关联、原文缓存等规则项汇总
- **列表窗口化**：可见字幕 ≥180 条时按滚动窗口渲染，减轻长片列表卡顿
- **重构/翻译注入原文**：无双语副轨时，语境重构与 Sakura/智能翻译自动尝试使用原文缓存作 source
- **工作区模式**：顶栏「精修 / 时间轴 / 双语 / AI」收起工具菜单分组；首次打开三步导览（可跳过，问号可重看）
- **时间轴磁吸**：拖字幕块时吸附播放头与相邻边界（按住 Alt 可暂时关闭）
- **双语规则审阅**：对照原文检查空译、疑似漏译、时长偏离、术语缺失（工具菜单 / 双语工作区）
- **Undo 补丁**：撤销栈相对打开时基线只存变更条，降低长片内存
- **QC Worker**：条目较多时质量检查徽标扫描改走 Web Worker（失败自动回退）
- **真 ASR 置信度**：任务完成后把 Whisper `avgLogprob` / `noSpeechProb` 写入 `.transub.json`；列表与详情区分 ASR / 启发式估计
- **ASS 说话人样式导出（Pro）**：按手动说话人着色导出 `.ass`
- **双语语义审阅（Pro）**：规则审阅之外可用 LLM 查漏译/错译（Advanced）
- **查找 Worker**：字幕较多时查找匹配改走 Web Worker
- **建议说话人**：按句间静音启发式交替标注（非声纹分离，可再手动改）

### 改进

- **Whisper 运行库无法一键修复**：根因多为 Windows「应用程序控制策略」拦截 `av` DLL（包已装仍导入失败）；检测会明确提示，并不再空转 pip；真正缺包时会强制补齐 `av` / faster-whisper / numpy


- **工作区工具条**：精修 / 时间轴 / 双语 / AI 切换时，相关工具直接显示在顶栏下方（不再藏在扳手下拉）
- **列表筛选扩展**：未看 / 已改 / 已通过，以及按说话人下拉筛选
- **跟播减负**：自动焦点跟播时跳过详情动作按钮重算与情境条刷新，并减弱列表 hover 闪烁
- **行状态色相**：播放行改用青色条（与紫色选中区分）；QC 行浅红条；深色主题同步拉开层次
- **列表脏行刷新**：打字改文时只更新当前行（含置信/标记），QC 徽标防抖；筛选切换跳过时间轴全量重绘
- **书签筛选**：列表可只看覆盖书签时间点的字幕
- **导出清单增强**：审校进度按全片统计（通过/已改/未看），并汇总书签与说话人标注覆盖
- **时间轴书签 / A-B**：书签以琥珀色钉显示并可点击跳转；A-B 区间半透明高亮
- **标记后轻刷列表**：审校/说话人变更时，有筛选则只重刷列表窗口，无筛选则只刷新相关行
- **列表筛选记忆**：重新打开编辑器时恢复上次筛选（含说话人；查找筛选不持久化）
- **工作区快捷键**：`Alt+1…4` 切换精修 / 时间轴 / 双语 / AI
- **工作流书签范围**：步骤 scope 支持 `bookmarks`；「当前筛选」对齐列表可见条目；内置「书签段精修」
- **行状态主题 token**：警告格 / 低置信 / 查找命中改用 `--ed-*`；播放中的 QC 行保留双色条
- **情境条 Pro 动作**：选中条可语境重构 / 智能翻译；有书签可「书签段重构」；有对照轨可语义审阅；按 QC 类型显示智能断句 / 修读速
- **导出清单 Pro**：未完成审校升为警告；汇总语义审阅结果与 ASS 说话人样式提示，并可一键跳转
- **语义审阅可跳转**：结果可点条目定位，并可对问题条批量语境重构
- **内置工作流「书签段重构（Pro）」**：仅对覆盖书签的字幕跑语境重构
- **双语设置对齐**：设置页「仅译文 / 译文在上」与编辑器统一为 `target` / `target-first`（仍兼容旧的 `translation*` 值）
- **番号识别库不透明扩充**：已知厂牌前缀改为不透明数据 `src/js/av-makers.am1`（+ 渲染层 `av-makers-embed.js`）；可用 `tools/fetch-av-makers.js` 联网合并 StashDB 厂牌列表与 JavDB Top250 等公开源后重新编码；商品解说文件名不再把厂牌号误判为强 AV
- **拖入感知更快 / 番号即感**：自动感知在拖入时只做文件名快感（不再等短窗语种 / 声学）；识别到已知厂牌番号等强 AV 证据时**立即完成并采纳**，不必等 ffmpeg 探测。点行内「重感」才启动深入感知（语种探测、声学、纠错记忆、模型列表刷新）
- **字幕默认与音视频同名**：翻译任务写出 `{片名}.srt`（不再用 `{片名}.zh.srt`）；合并双语默认也写成与影片同名，便于播放器自动挂载
- **设置导航分组**：侧栏不再切换「常用 / 专家」，改为「常用」与「Pro」两组；原专家页（历史、字幕处理、性能、参数等）并入常用，Pro 许可与大模型单独列出
- **语调适配词表/提示词不透明化**：日语软声词表与 Sakura / 忠实语气成人向 system prompt 改为 `src/js/tone-adapt.tz1`（zlib + XOR），加载模块为 `tone-adapt-lexicon-core`；源码侧不再明文展开；可用 `tools/encode-tone-adapt.js` 编解码维护

### 修复

- **打开字幕自动关联同目录媒体**：同文件夹下按同名 / 去轨后缀（`.zh` / `.src` 等）匹配视频或音频；历史失效路径会回退重新猜测；多点片名（如 `Show.Name.S01E01.zh.srt`）不再误切成首段
- **双语重转写覆盖译文**：当前主轨为译文时，普通重转写将 ASR 结果写入原文对照轨（不再覆盖译文）；进度/预览/右键菜单会提示；改译文请用「重译」或「双语重跑」
- **日语 AV 软声/呻吟丢失过多**：灵敏检出与 `av_soft` 提高弱音召回——WhisperSeg 阈值更低、ASR `no_speech`/`log_prob`/压缩比放宽、幻觉静音默认 4s；后处理不再把中等长度呻吟当噪声丢弃（仅压极端叠字）；低置信过滤真正提高 `no_speech` 容忍度
- **短窗语种探测缺 numpy**：dist 仅含 HTTP 最小依赖，Whisper 权重下载后未自动装 `faster-whisper`/`numpy`，感知语种会报 `No module named 'numpy'` 并跳过；现于下载模型与语种探测时自动补齐 Whisper 运行库，且 zip 更新保留 `numpy` 等传递依赖（避免只留下 `faster_whisper` 却丢 numpy）；设置向导检测步骤也会显示 numpy / faster-whisper 就绪状态
- **whisperseg-asmr 必装**：设置向导与各档位「下载所需模型」预勾选均包含 WhisperSeg ASMR（灵敏检出）；向导下载列表与环境模型卡标注「必装」

### 新功能

- **界面缩放（4K）**：设置 → 常规 →「界面缩放」支持自动 / 100%–150%；默认「自动」在 4K 级物理分辨率且系统缩放偏低时适度放大界面文字，系统已 ≥150% 时不叠加；主窗口、设置、字幕编辑器等窗口统一生效
- **zip 解压版应用内更新**：`win-unpacked` / `Transub-*-win.zip` 可在「检查更新」中下载并重启安装；覆盖外壳时保留 `transub-engine/models` 与已安装的 GPU / Demucs 等 site-packages（重命名旁路，不二次拷贝大文件）
- **统一下载中心**：引擎模型 / GPU / Demucs、Advanced GGUF / 运行时、应用更新共用「下载管理」窗口；纯下载列表（每任务独立进度条、速度与已下载/总大小、取消/重试图标）；支持跨类型并行、状态落盘、失败重试、主界面迷你进度与托盘提示
- **内置 Engine 独立版 dist**：发行包 / 源码树包含 `transub-engine/`（`runtime\python.exe`，无需本机 Python / 无需 `install.bat`）；`engineInstallPath` 留空即用；设置「环境」可一键「内置」；引擎不再携带 ffmpeg（复用 Transub `_internal/bin`）
- **任务资源占用条**：任务进行中可在进度条下方显示整机 CPU / 内存占用；检测到 NVIDIA 时追加 GPU 与显存。设置 → 常规 →「任务进行时显示资源占用」可开关（默认开）
- **按项自动感知**：主界面「感知」开关开启后，拖入的每个视频/音频在探测完成后自动启发式分析（文件名 / 路径 / 时长），行内显示类型与方案；**有结果时默认采纳**，可「重新感知」「不采纳」。开始生成时按文件合并已采纳的语种 / ASR / MT / 音频·VAD 覆盖（不改任务与设备）。替代原设置项「开始前自动匹配素材类型」
- **感知缺项安装提示**：开始生成时若已采纳感知方案但首选模型 / Demucs 等支持项未安装，弹窗询问「去安装 / 仍用现有继续 / 取消」；确认后勾选并下载缺失项（或打开 Demucs 窗口）
- **短窗语种探测**：感知时若表单语种为自动且无音轨/文件名强先验，则调用引擎 `POST /v1/detect-language`（Whisper 编码器，约 12 秒窗口，默认 `startSec≈60–180` 跳过片头配乐）写入该项语种覆盖；音轨 metadata 与文件名假名/韩文/已知番号等为更快先验；弱置信或片头区结果不覆盖更强先验
- **感知模型匹配**：按素材类型 + 语种在已安装模型中择优（如日语 AV 优先 `whisper-ja-1.5b`、已装则优先 Sakura、否则 `opus-mt-*-zh`；未安装则回退或交引擎自动选）。混批中单项 Sakura 覆盖也会拉起外部 MT 适配器
- **声学轻探测**：感知阶段对非强 AV 素材做约 12 秒 volumedetect/silencedetect（同样跳过片头）；偏配乐倾向影视增强（或免费降噪），偏软声稀疏倾向灵敏检出，偏底噪倾向轻度降噪
- **感知纠错记忆**：采纳/不采纳会记住目录与厂牌番号偏好，下次同目录/同厂牌自动抬升或避免自动采纳；设置 → 常规可查看条数并一键清空
- **素材类型识别**：日语 AV · 影视 · 访谈。**AV 优先**：已知厂牌番号（SSIS/MIDV/FC2/200GANA 等）与 AV 关键词为强证据；商品「货号+产品介绍」等弱信号改判对白·访谈，且不会压过强 AV 证据
- **免费 LLM 管线翻译**：智能翻译在选用软件内白名单 ≤7B GGUF（如 Qwen2.5 3B/7B）时可免费使用；「忠实语气」亦可免费使用；BYOK、更大模型、语境/影片重构与影视音频增强仍需 Advanced
- **引擎外部 MT（LLM）适配**：Sakura / Advanced 智能翻译改为走引擎 `mtBackend=external`；本机 HTTP 适配器承接 cues，引擎负责双语输出与 MT 前显存释放（不再先 `transcribe` 再事后改写字幕文件）；术语表改由适配器写入 LLM prompt（与字幕编辑器一致），不再做 Opus 式 `__GLOSS*` 占位保护
- **引擎 ASR 叠名 / Opus 人名轻清洗**：引擎在 MT 前剥离 Whisper「玲奈玲奈 / 葵葵葵」等叠名与整句名渣；Opus 日→中译后轻量去掉无源支撑的句末/句中污染人名（佳奈、美咲桑等）；桌面文件级清洗仍作兜底；external LLM 仍走适配器全量清洗
- **日语 AV / 软声预设**：内置「日语 AV · 软声翻译（WhisperSeg）」与可选「JA1.5B」预设；灵敏检出对齐引擎最新 NSFW 实测（静音 160ms、幻觉静音阈值 2s；关闭降噪/Demucs）；ASR 可选 `whisper-ja-1.5b`
- **设置向导**：主界面与设置页可打开向导；按任务需求与硬件检测自动推荐并写入设备 / 档位 / 模型与下载源；首次无配置文件时自动弹出；完成后可打开模型下载窗口；第 5 步应用下载进度显示已下载 / 总大小与速度
- **设置独立窗口**：设置与设置向导均在独立 BrowserWindow 中打开（不再使用主界面内嵌弹层）
- **字幕编辑器窗口菜单**：按文件 / 编辑 / 工具 / 批量 / 质检 / 视图 / 帮助归类常用功能；仅显示自定义菜单，不展示 Electron 原生菜单项；视图菜单以勾选状态显示自动焦点、波形时间轴、深色主题，并按缩放状态启用「适应窗口」
- **Advanced 基础框架**：买断许可（2 台设备、30 天换机冷却、30 天复核）、BYOK 设置、闭源模块加载点；工作流步骤「语境重构（Advanced）」已挂载（需许可 + 闭源模块）
- **语境重构（内置）**：BYOK 调用 OpenAI 兼容接口，分块改写并回写字幕；支持模拟模式与术语/双语对照；开发未打包时自动解锁便于测试
- **批量语境重构**：设置中已移除「完成后语境重构」；请在字幕编辑器内使用语境 / 影片理解重构
- **重构对照增强**：对照弹窗可点击条目跳转并定位播放；右侧结果可编辑；「仅显示变更」偏好会记住
- **重构进度 ETA**：分块处理时显示预计剩余时间
- **影片理解：术语与强度**：开始前可读取术语表预填译名；可选轻度 / 均衡 / 加强改写强度
- **Film Brief 预览**：生成 Brief 后可编辑确认，再开始场景改写
- **失败块重试**：场景块回退时，对照弹窗可一键重试失败条目
- **增量刷新列表**：确认替换后逐行刷新（大量变更时仍整表刷新）
- **本地模型空闲释放**：任务结束后 5 分钟无新任务再停止 llama-server
- **模型选择内存提示**：根据系统内存对可能吃紧的模型显示「内存紧张」
- **影片理解重构（新）**：独立功能；先生成 Film Brief，再按场景分块改写，更贴合整片；原「语境重构」保留不变；开始前可填写片名 / 简介 / 译名约定（按字幕文件记住）；完成后弹出左右对照，确认后再覆盖
- **大模型双通道**：Advanced 可选用「外接模型（BYOK）」或「软件内选模型」（下载 GGUF + 自带 llama-server 本地推理）；模型/运行时下载纳入统一下载管理（进度、任务列表）；软件内模型提供「性能测试」（tok/s）
- **本地模型选择窗口**：设置页改为摘要 +「选择 / 管理模型」；独立窗口列出更多 GGUF（Qwen / Llama / Gemma / Phi / Mistral / DeepSeek），支持搜索与系列筛选
- **智能翻译（Advanced）**：字幕任务「翻译 / 双语」可勾选；先转录原文，再用高级里配置的大模型翻译（不走 Whisper 翻译模型）
- **影视音频增强（Advanced）**：进 ASR 前人声分离（Demucs）+ 电影级 VAD；免费侧提供「更激进切分」「轻度降噪」与完整 VAD 参数透传
- **免费音频切分增强**：引擎实际应用 VAD 阈值/时长；可选轻度 ffmpeg 降噪
- **更多免费 MT 模型**：设置可选 KO/DE/ES/FI/SV→中（与 EN/JA 一并列出）；「自动」按源语言选择；档位下载含韩中
- **免费 Sakura 译中**：翻译方式三选一（引擎 Opus / Sakura / Advanced 智能翻译）；Sakura 可选 `sakura-1.5b`（默认轻量）/ `sakura-7b`；引擎只 ASR 后用本地 GGUF 日→简中（llama-server，无需 Advanced）；模型许可 CC-BY-NC-SA
- **模型下载可选列表**：「下载所需模型」弹窗列出 ASR/MT/VAD/Sakura，可勾选后再下载（预勾选当前档位与所选模型）
- **引擎输出增强**：字幕格式支持 ASS；双语任务「合并」→ 引擎写出 `*.dual.ass`；可选词级时间戳 / 卡拉 OK WebVTT；可强制任务后释放引擎显存
- **引擎术语强制译名**：全局术语表「别名 → 标准名」可传给 Opus MT（与 TWAI 的 ASR 提示分开）
- **仅影视 VAD 预设**：Advanced 可只套用影视向 VAD 默认而不做 Demucs 人声分离
- **幻觉静音阈值**：转录专家项可设 Whisper `hallucinationSilenceThreshold`；「翻译 · 严切分」预设默认 2 秒
- **编辑器重转录**：继承转录 Tab 的 VAD / Beam / 轻度降噪设置

### 修复

- **CUDA 12 支持组件下载卡住**：根因是 `ensure-gpu-stream` 等 SSE 接口把整段安装跑完才一次性推送进度（界面长时间无反馈）；已改为线程内实时推送。同时默认改用阿里云 PyPI（本机托管 win_amd64 whl），失败回退华为云 / 清华；pip 进度解析已下载/总大小/速度并在向导中显示
- **向导下载进度始终 0 B**：CUDA pip 轮子先落在临时目录，目录监控与 pip 行解析（含 `123.4M/360.1M`）未跟上时会把 `0` 当成真实进度；现监控 pip 临时目录、修正解析，并向导忽略 0 字节、保留上次有效大小/速度
- **向导总大小随已下载一起变**：进度超过预估时曾把 `totalBytes` 抬成当前已下载，界面变成不断增长的 `X / X`；现总大小保持预估/官方公布值，不再跟已下载对齐
- **向导已下载大于总大小**：pip 在 100% 进度行上重复累加已完成 wheel，且临时目录与安装目录字节被加总；现按单次提交累计、以 pip 文本为主、目录只取 max 回退，并在换模型/阶段时重置界面粘滞总大小
- **任务完成后操作未执行**：Transub Engine 批处理结束后应执行「关机 / 退出 / 睡眠 / 打开目录 / 提示音」，但 `runPostSubtitleTaskActions` 未从 bridge 导出且引擎侧会剥掉 post-task 字段，导致调用失败被静默吞掉；现已导出并在开跑时同步 session、写入输出目录
- **任务进度停在 99%**：任务结束后先清除 `running` 再刷新进度条，避免仍按运行中封顶 99%，后处理结束后也不会再卡在 99%
- **任务历史为空**：Transub Engine 批处理结束后写入 `transub-task-history.json`（此前仅 TWAI 路径会记录，导致默认引擎完成后「任务记录」仍为空）
- **LLM 译后清洗**：Sakura / 智能翻译批次输出自动压缩叠词循环、剥离 `Gloss*`/`__GLOSS*` 泄漏与尾部臆造人名，并收紧 `max_tokens`、启用 repetition/frequency penalty；Sakura 多余输出行改为截断而非并入末行；引擎批处理叠词压缩默认与 TWAI 一致（`!== false`）
- **文件级译后清洗**：引擎任务结束后对 `.zh.srt` 对照 `.src.srt`（或 `result.cues.source`）再跑一遍清洗，覆盖 Opus 内置 MT 与适配器漏传选项的路径，避免句末佳奈/玲奈/真理等残留
- **ASR 叠名幻觉清理**：剥离 Whisper 在日语轨上的「玲奈玲奈 / 葵葵葵 / 琴音琴音」等连续叠字；外部 MT 译前与杂音后处理默认启用；叠名不得再作为译名合法性证据
- **译中人名误用加强**：文中无源支撑的污染名（玲奈/美咲桑等）整段剔除；句末污染角色标签一律去掉；真名仅保留句内且源文敬称/片假名等强证据
- **译后人名一致**：清洗阶段按术语表别名→标准名统一称呼；无表时据日文「さん/くん」共现，将同人不同译法（如「仓木同学 / 仓木小姐」）归并为多数写法
- **译后污染加强**：循环剥离文末臆造人名/角色标签（含「汤米 舞」「… 月」），源文不支持时去掉；清理片假名长音残留「ー」；Sakura 将「。」等纯标点译文视为空白并重试
- **译后污染（ABF）**：剥离 LLM 提示词回声（「将下面的日文…只输出译文」）；单字文末角色标签（玲/舞/陆等）；`うまい` 不再误判为「舞」；残留假名短句与提示词泄漏视为空白并重试；清理无成对开引号的 `」`
- **日文人名词典**：新增 `ja-person-names-core`（常见名假名/汉字→中文面、污染名、常见姓）；源文敬称/自报姓名/词表命中可证明译名合法；文末臆造名识别覆盖美咲/结衣/阳菜/佐藤等更广集合
- **人名误判修复**：语气助词「～かな」不再证明「佳奈」；「うまい/マイク」不再证明「舞」；污染高频名（佳奈/舞等）需敬称/自报/片假名等强证据才保留；自报规则不再把「～んです」当成名字
- **语调适配词表**：新增 `tone-adapt-lexicon-core`（喘息/湿声拟声、身体与行为俚语→汉化组译法）；Sakura 成人向提示 / 忠实语气按原文命中自动注入术语；译后可把残留假名拟声替换为中文
- **ASR 日语断词拼接**：后处理在删碎片前，将「…ご」+「めんなさい」、「…少」+「しだけ」、「こで…」等紧邻断词条合并（默认随「清理杂音」开启）
- **Sakura NSFW 提示词**：日语 AV 感知采纳 / `ja-av-*` 预设 / 忠实语气时自动启用精简 NSFW system prompt（禁净化、拟声中文化、碎句保留、禁造名与叠词）；显式 `sakuraNsfwPrompt: false` 可关闭
- **Sakura 生成质量低于编辑器**：生成管线对 Sakura/智能翻译跳过引擎内置人名表与 `__GLOSS*` 术语占位保护，改为像字幕编辑器一样把干净日文 + 术语表交给模型
- **灵敏检出 + SenseVoice**：WhisperSeg 仅支持 Whisper；勾选灵敏检出时自动切到 Whisper ASR；若 ASR 仍为 SenseVoice 则回退 `fsmn-vad`，避免 FunASR「model is not registered」失败
- **设置可读性**：常规页增加三步上手指引；翻译方式改为「引擎 Opus / Sakura / Advanced 智能翻译」三选一；环境页增加配置顺序与主次按钮；Advanced 页标明「立即生效」；未保存徽标 +「保存 / 保存并关闭」分离
- **引擎输出路径识别**：正确识别引擎 `zh` / `dual` 角色输出（此前只认 `target`，导致译文路径丢失）
- **LRC 与引擎**：LRC 仅对 TWAI 显示；默认引擎路径不再静默把 LRC 当成 SRT
- **设置易混淆项**：环境条幅改为打开「环境」页（另附向导入口）；「高级」标签更名为 Advanced（许可），与编辑器区分；`advanced` 别名指向 Advanced 而非编辑器
- **空引擎路径被写成开发机目录**：不再把空路径保存为 `F:\Transub-Engine`；首次需用户明确填写
- **预设 / 导入配置**：套用后明确提示仍需点「保存设置」；编辑器打开设置默认落到「常规」
- **灵敏检出**：切换 VAD 为 whisperseg-asmr 时给出状态提示
- **Sakura 翻译在 Whisper 后崩溃**：转写结束后先释放/停止引擎以腾出显存，再启动 llama-server；GPU 启动失败时自动降层并回退 CPU，并保留 stderr 便于诊断
- **影视人声分离显示 CPU**：任务会把「GPU」显式传为 `cuda`；下载 Demucs 时会补齐 CUDA torch；若仍显示 CPU，日志会写明原因（无 GPU / torch 无 CUDA 等）
- **影视音频增强勾选不保存**：`normalizeTransWithAiRuntimeOptions` 与保存白名单已纳入 `filmAudioEnhance` / `vadAggressive` / `audioLightDenoise`
- **智能翻译漏翻**：模型漏 index 时自动补翻（最多 2 轮）；强化「必须全覆盖」提示；默认分块从 28 调至 20
- **智能翻译忠实语气**：可选「成人向 / R 级」模式，保留粗口与成人语义；模型拒答时自动用该模式重试（免费可用，不限 Advanced）
- **Demucs 下载入口**：设置 → 环境 → Transub Engine 增加「下载 Demucs」按钮；有 NVIDIA GPU 时会强制重装 CUDA 版 PyTorch（优先阿里云 cu126 直链 whl；避免 CPU 版 2.13 挡住旧 cu124，以及官方 CDN 在国内卡住）
- **Demucs / CUDA torch 被「智能应用控制」拦截**：若状态提示无法加载 torch DLL（WinError 4551），请关闭 Windows 安全中心 → 应用和浏览器控制 → **智能应用控制**，或将引擎 `.venv` 加入排除项后重启

- **影片理解/语境重构后编辑器卡顿**：任务结束后退出本地 `llama-server`（避免模型常驻占显存/内存）；并忽略其子进程 stdout/stderr，防止管道缓冲撑爆主进程

## 2.0.0

### 重大更新

- 软件架构进行了大量的优化，使性能更进一步提升。
- 重构后的界面，将常用操作集成在主窗口内，使操作更便捷。
- 增加大量实用新功能（双语字幕、字幕预设、工作流等）。

### 新功能

- **双语任务**：单次任务即可输出原文和译文，可选合并输出双语字幕；（双语功能需分别下载和设置转写/翻译模型）
- **导出繁体中文字幕**：主界面设置目标为繁体中文时，可生成繁体中文字幕
- **独立的字幕编辑器启动项**：可通过Transub Editor图标快速启动字幕编辑器，快速查看/编辑历史字幕文件
- **字幕预设**：编辑器支持按时段批量插入片名/演员/字幕作者等预设文案
- **工作流**： 创建你自己的工作流，将重复流程用工作流快速批处理。（已内置两套常用工作流）
- **双语字幕编辑**： 支持双语字幕编辑，可设置原文和译文的上下位置，支持双语字幕合并。
- **任务历史：** 支持历史任务的查看、编辑和清除

### 性能 / 启动体验

- 优化了软件冷启动速度
- 优化了任务文案，更好的理解任务状态
- 优化了字幕编辑器波段操作时的卡顿现象
- 重构了主界面UI，各项操作、信息更一目了然。
- 重构了设置界面，分为标准和专家两种设置模式。新手不必再为看到一堆复杂的设置项头痛了。
- 配置预设，可将各项设置项进行存储，便于各种不同情况下快速切换配置项。

### 其它

- **推荐安装**：下载 `Transub-2.0.0-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-2.0.0.exe`

## 1.4.4

### 修复

- **Smart App Control 拦截**：关闭 electron-builder 向 exe 写入 asar integrity（该改写会让未签名的 `Transub.exe` 在 Windows 11「智能应用控制」强制模式下被拦截）；仍保持未签名，图标/版本信息继续由 after-pack 写入



### 其它

- **推荐安装**：下载 `Transub-1.4.4-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.4.exe`



## 1.4.3



### 修复

- **打包缺词典**：`subtitle-chinese-core` 依赖的 `subtitle-chinese-dict` 未打入 `app.asar`，简繁转换仍会失败；打包清单改为纳入全部 `*-core.js` 与该词典



### 其它

- **推荐安装**：下载 `Transub-1.4.3-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.3.exe`



## 1.4.2



### 修复

- **打包缺模块**：正式包 `app.asar` 未打入 `subtitle-chinese-core`，批量完成后做简繁转换时报 `Cannot find module '../src/js/subtitle-chinese-core'`



### 其它

- **推荐安装**：下载 `Transub-1.4.2-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.2.exe`



## 1.4.1



### 新功能

- **压缩叠词**：将连续重复 ≥ 3 次的短语/单字压成「词…词！」（如「好的」×5 →「好的…好的！」），降低 CPS；编辑器工具菜单、当前条目工具栏、QC 一键修复均可用



### 设置

- **完成后压缩叠词**：设置 → 编辑 → 批量完成后；默认开启，写回字幕
- **完成后自动按 CPS 拆句**：默认改为开启（此前默认关闭）



### 字幕编辑器

- **列表全文显示**：字幕列表文本不再两行截断，长句可完整换行查看
- **QC 叠词检测**：质量检测可标出叠词嫌疑，一键修复可选压缩叠词



### 其它

- **推荐安装**：下载 `Transub-1.4.1-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.1.exe`



## 1.4.0



### 题外话

- **更新频率**：自项目开始至今几乎保持着每天更新的节奏，但目前各项功能都已较为完善且稳定。**后续会大幅度的降低更新频率**。
- **新功能**：目前各项功能已经非常强大，尤其是字幕编辑器的功能已经强于大部分同类字幕编辑器。目前软件所呈现出来的功能已经是我所能想到的几乎所有想法了。**所以对于新功能，更多的是需要依靠网友们的想法和建议了。**
- **MAC版本：**暂无MAC版本的开发计划（有开发MAC版本的开发者可借助项目开源代码只需微做适配即可制作出MAC版本）



### 新功能

- **波形编辑器：支持放大缩小波形尺寸，通过拖动、拉拽等方式快速调整字幕时长**
- 全局术语表：在翻译模式下，自动应用全局术语表（在设置内开启，默认关闭）
- 抑幻听：通过设置，可极大程度减少幻听、杂音、错误转录BGM等情况
- 自动拆句：对于单条过长的字幕按语句自动拆分（需在设置中 开启 **自动按CPS拆句**）
- 生成繁体字幕：现在可以直接生成繁体中文字幕了（在设置的输出项内的中文字幕类型，选择 繁体中文）



### 主窗口

- **独立设置窗口**：设置改为独立 BrowserWindow；从字幕编辑器打开设置时不再顺带唤起主任务窗口



### 字幕编辑器

- **波形默认开启**：打开编辑器时默认显示波形时间轴（仍可关闭；本地已保存的开关偏好优先）
- **时间轴/波形右键菜单**：在时间轴字幕块与波形对应区间上右键，可打开与字幕列表相同的上下文菜单（分割、对齐、时长调节等）
- **删除杂音**：新增「幻觉短句」规则（完毕 / ○○○ / 过短占位等）



### 设置

- **设置分区重排**：8 个页签收为 6 个——**常规**（预设/任务/设备/托盘）、**环境**（TransWithAI + FFmpeg）、**输出**、**转录**、**编辑**（编辑器 + 批量完成后）、**更多**；各区分组与说明文案更清晰，打开设置默认进入「常规」
- **中文字幕类型**：输出页可选简体中文 / 繁体中文（默认简体）；仅「翻译」任务生效，完成后用 OpenCC 做简繁转换（不再写入 initial_prompt，避免「简体中文」等提示被复述进字幕）；独立设置窗口保存后会同步到主窗口，后处理以磁盘配置为准，避免仍按旧的「简体」转换
- **抑幻听参数**：转录页可调无语音/对数概率/压缩比阈值，以及可选的幻听静音跳过；写入 TransWithAI `generation_config`，便于 BGM/静音较多时减少胡编字幕
- **内置预设「翻译 · 抑幻听」**：日语锁定 + 更严的无语音/压缩比阈值 + 较短合并上限（8s）与智能切分 20s，适合 BGM 或静音较多的内容
- **完成后自动按 CPS 拆句 / 清理杂音幻觉**：设置 → 编辑 → 批量完成后；可选写回字幕，缓解单条过长与「完毕」类短句
- **翻译句读后空格**：翻译任务完成后，在 CPS 拆句之前自动在中文 `。？！` 后补空格（已有空白/行末不重复），便于拆句识别句界
- **术语表写入提示**：默认将全局术语写入 `initial_prompt` / `hotwords`（generation_config）
- **参数试跑对比**：设置 → 更多；对勾选视频片头用两套预设各跑一遍并对比 QC



### 平台与分发

- **发行包调整**：处于 portable 版本的运行体验较差，故后续版本仅提供 `Transub-*-win.zip` 与 `Transub-Setup-*.exe`，不再提供 portable 便携版



### 其它

- **推荐安装**：下载 `Transub-1.4.0-win.zip` 解压后运行 `Transub.exe`，或使用 `Transub-Setup-1.4.0.exe`



## 1.3.2



### 平台与分发

- **打包资源封入 asar**：HTML / JS / CSS 不再解压到 exe 旁明文目录，改为打入 `app.asar`，降低被随意篡改的风险（`_internal` 等原生依赖仍保持旁路）
- **默认分发改为 zip + 便携版**：未签名时 NSIS `Setup.exe` 易被 SmartScreen / 杀软拦截；Release 主推 `Transub-*-win.zip`（解压即用）与 `Transub-*-portable.exe`。NSIS 安装包仅作可选本地构建（`npm run build:setup`），不再作为默认发布产物



### 主窗口

- **记住窗口尺寸**：关闭后再次打开恢复上次宽高、位置与最大化状态
- **新版本检查**：更加方便的检查软件新版本，快速用上新功能和bug修复



### 设置

- **默认参数调整**：与常用配置对齐——字幕格式默认仅 SRT；合并间隔 500ms / 最大时长 10s；默认不生成 .bak；托盘进度默认关；完成后 QC 检测默认开
- **系统托盘通知开关**：运行页可开关桌面通知（任务完成 / 最小化提示），默认关闭
- **检查新版本入口**：主窗口设置按钮旁增加图标，可直接检查更新



### 字幕编辑器

- **时间轴/波形可缩放平移**：默认 5× 显示；Ctrl+滚轮或按钮放大缩小，滚轮/滑条左右滚动；倍率写入本地偏好，下次自动恢复
- **新增时间轴/波形区字幕拖动调整时长**： 调整字幕时长更方便了
- **波形加载提示**：开启波形时显示进度遮罩与状态栏提示，避免长视频抽峰时界面无反馈
- **.bak 备份位置可配置**：设置 → 字幕编辑器；可选不生成（默认）、与字幕同目录、或软件数据目录下的 `backup` 文件夹
- **静音分割更精准**：粗定位 + `atrim` 精确取窗（避免关键帧偏移）；单声道高通预处理；切分点按「靠近文本断点 + 静音时长」评分；相邻条目按静音边缘分开
- **静音分割对齐断句词**：理想切点合并空格/换行、断句词与标点；连续无空格文本若匹配断句词也可静音分割



### 修复

- **确认框屏闪**：关闭确认/取消后不再 `blur` 再抢焦，避免短暂露出下层主程序窗口
- **任务栏图标**：开发模式 (`npm start`) 会把 Transub 图标写入 `electron.exe`，避免重装 Electron 后又变回默认图标；从托盘恢复时也会重新应用窗口图标；最大化改在 show 之后，避免任务栏显示宿主 exe 图标



### 其它

- **推荐安装**：下载 `Transub-1.3.2-win.zip` 解压后运行 `Transub.exe`，或使用便携版 `Transub-1.3.2-portable.exe`（均未签名，一般不会像 NSIS Setup 那样被系统拦截）



## 1.3.1



### 主窗口

- **托盘进度与 ETA**：任务进行中更新托盘提示；进度条文案显示已用时间与预计剩余（基于片长与历史吞吐）
- **托盘相关设置**：运行页可开关「任务时更新托盘进度」「开始时最小化到托盘」
- **完成后 QC 检测**（默认关）：设置 → 字幕编辑器；任务结束后仅扫描并标出问题数，不自动修复、不自动打开编辑器
- **启动加速**：窗口 `ready-to-show`、桥接按需加载；FFmpeg / TransWithAI / GPU 检测改为后台快速检查
- **任务队列**：并行探测视频；列表增量刷新；排队完成后确认是否继续
- **设置分区**：原「高级」改为「FFmpeg」；新增「更多」（导入/导出配置、检查更新、任务历史、访问官网）
- **检查更新**：安装包将主进程共用的 `src/js/*-core` 打入 asar，修复「检查软件更新」因缺少 `subtitle-glossary-core` 报错；下载进度条显示
- **取消任务**：Windows 下 `taskkill /T` 结束 infer 进程树
- **任务完成后**：可打开输出目录、播放提示音；GPU 建议可一键应用设备



### 字幕编辑器

- **多选批量**：Ctrl/Shift 点击多选；支持批量删除、合并相邻、简繁/偏移作用于选中；`Ctrl+A` 全选可见行
- **长任务可取消**：静音批处理 / 贴边 / 重转写进度条增加取消；Esc 可中止；FFmpeg 进程可终止
- **可选波形时间轴**：工具栏开关（默认关）；开启后从关联视频抽峰绘制在时间轴下方
- **自动备份与草稿恢复**：保存时写 `.bak`；脏状态定时草稿；重新打开时若草稿更新可选择恢复
- **项目术语表**：字幕旁 `{stem}.glossary.json` sidecar；编辑器术语表弹窗可切换全局/项目，扫描与统一使用合并后的有效术语表
- **简繁短语转换**：OpenCC 短语优先 + 字符映射；可选保护术语表词条（默认开启）
- **批量简繁转换**：工具菜单 → 文本 →「简繁转换」，支持简→繁 / 繁→简，可作用于全部或当前选中条目（可撤销）
- **Ctrl+↑/↓**：文字编辑框内切换上一条 / 下一条字幕
- **快捷键**：工具栏独立按钮（设置后方）



### 修复

- **设置不持久**：打包后用户设置改为写入稳定目录（NSIS → Electron `userData`；便携版 → `PORTABLE_EXECUTABLE_DIR`），不再写到每次解压的临时 exe 旁；并自动迁移旧位置的 `transub-settings.json`（感谢QQ804218提供的bug信息）
- **FFmpeg 目录“消失”**：自定义路径若落在软件安装/`_internal` 内，更新或重装会覆盖该树；现会提示改用安装目录外的路径，且检测保存失败时不再静默忽略
- **查找替换**：全部替换成功后显示「已成功替换 N 处」，不再误报「未找到匹配项」
- **确认框失焦**：确认/取消后恢复编辑器输入焦点（避免需切窗才能继续编辑）
- **任务栏图标**：开发与打包路径正确使用 Transub `app.ico`
- **日志滚动**：转录过程中，日志自动滚动至最新条目（感谢QQ804218的建议）



### 平台与分发

- **GitHub 应用更新**：设置页「检查更新」对接 GitHub Releases
  - NSIS 安装版：若 Release 含 `latest.yml`，可应用内下载并重启安装（electron-updater）
  - 便携版 / 开发模式 / 无 `latest.yml`：通过 GitHub API 比对版本并打开下载页



## 1.3.0

- 字幕编辑器：QC、通顺度、术语表、时间轴、静音/智能分割、局部重转写等
- 内置 FFmpeg；Windows 一键打包脚本



## 1.2.0

- 多种分割模式；共享分割核心；静音检测



## 1.0.x

- 初始基于 TransWithAI 的批量字幕生成与基础编辑

